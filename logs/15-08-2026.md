# 15-08-2026

## 00:17:54 UTC · `e2677dd3`

**covmeet2** — storage redesign after the user's 3-min smoke on the rented 40-core box (77 st/s steady, verify exit 0, but the events file read as far too heavy). v1's per-edge log (parent pair repeated per row, rows written even for no-op re-reaches) was ~85% of bytes and the superlinear growth term; v2 stores results and resume state only: one `o` row per orbit ever, `r` only on mask growth, census by index, **full chains only on `merge`/`drop` certificate rows** (parents RAM-only; post-resume chains may be `truncated` — the deterministic re-run recovers them). Relators >120 letters pack 2-bit+b64. Engine tag `covmeet2` in the filename; v1 files can never resume under v2. Measured: 52 KB per 247 expansions (~4× less than v1 at equal depth, before the growth-term saving). Measured at the active shells (L 19–21, uncapped): children top out at 59 letters — the smoke's reported 367 MB is arithmetically inconsistent with its own 68,277-edge count (~10 MB); diagnostic requested from the box.

**Changed:** [`covmeet.py`](../experiments/stable_ac/cov/meet/covmeet.py), [`verify_covmeet.py`](../experiments/stable_ac/cov/meet/verify_covmeet.py) (now certifies every chain segment-by-segment), [`meet/README.md`](../experiments/stable_ac/cov/meet/README.md), [`tests/stable_ac/test_covmeet.py`](../tests/stable_ac/test_covmeet.py) — 21 passed. Notebook untouched (same API); exec-checked fresh + resume + verify against this commit.

## 00:43:11 UTC · `PENDING`

**covmeet3** — the user asked for still-smaller storage ("hashing or other techniques") and for maximum throughput for a 24 h run on the rented 40-core box. Storage: replaced the v2 journal with an **atomic bounded snapshot** (whole store 2-bit-packed with masks, census, parents+moves; rotate→prev, sha256, corrupt-falls-back) + a **certs-only jsonl** (meta/seed/merge/drop with full chains). ~18 B/orbit measured, disk bounded at ~2 snapshots regardless of runtime; chains now survive crashes (parents persist). Hashing declined for identity: a collision-safe digest is 16 B, the exact packed pair is 5–10 B — exactness is smaller than hashing. Speed: measured `aut_min` = 89% of per-state cost and ~66 raw children → ~21 `relabel_min` groups → `_aut_min_memo` (exact — equal relabel keys share the Aut-orbit): **134 → 51 ms/state cold, 21 ms warm**, expected 3–4× on the box. Verifier is memo-free and now checks sampled orbits are `aut_min` fixed points.

**Changed:** [`covmeet.py`](../experiments/stable_ac/cov/meet/covmeet.py), [`verify_covmeet.py`](../experiments/stable_ac/cov/meet/verify_covmeet.py), [`meet/README.md`](../experiments/stable_ac/cov/meet/README.md), [`tests/stable_ac/test_covmeet.py`](../tests/stable_ac/test_covmeet.py) — 24 passed; notebook unchanged and exec-checked (fresh → resume → verify exit 0; folder 39 KB).
