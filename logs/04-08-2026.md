# 04-08-2026 — S-line session log (fable line)

Branch `claude/stable-ac-conjecture-stabilization-rwo9as`, started from the fable head
`b6333ca`. **This branch must be merged into `fable/proof` by the user** — cloud sessions
can only push to their own `claude/*` branch. No PR opened (`FRAMING` trap 10).

Session brief: research the stable AC conjecture with **arbitrary numbers of stabilizations
(rank ≫ 3)**; test whether hard presentations — AK(3), and the 124 unsolved Miller–Schupp
AC-classes — get easier at ~10 generators; use **change of variables** and **Lemma 11**;
push theory first, then run code on the most promising subset with modest budgets.

## 06:31 UTC · Orientation and the opening hypothesis

Identified the target set: `data/ms_unsolved_reps/aca_124.csv` on
`origin/cursor/heur-u124-s20mk2-a42e` holds the 124 AC-classes (`aca_0..aca_123`) covering
the 261 unsolved MS representatives; `aca_115` = `YXYxyx,YYYYxxx` **is AK(3)**.

Opening idea (`S0_HIGH_RANK_PLAN.md`): the free-definition move — after AC4 adjoins `(z,z)`,
the relator `z` may be redefined to `z·w` for **any** `w`, because the group is trivial —
lets any balanced presentation be *triangulated* to relators of length ≤ 3 at rank
`n + Σ(|rᵢ|−3)`. For AK(3) that is **rank 9**, which is exactly the "maybe 10 generators"
of the brief. Payoff chain: a thickenable member at rank 9 ⇒ Lackenby Thm 1.3 ⇒ AK(3)
stably AC-trivial.

Hand-built AK(3)'s rank-9 triangulation, census 86,400, `minimum_defect` 4 in 0.9 s.

## 06:44 UTC · The opening idea dies, and the theorem that kills it

Positive ladder: 63 AC-trivial γ_N = 0 bases, fully triangulated to ranks 3–9 — γ_N = 0
preserved 63/63. Falsification run: 480 random triangulations of 13 positive-defect bases
(120 of them AK(3)) reproduced the base defect **exactly, zero deviations**, and 0/12 bases
ever reached 0.

**Theorem S3.** A chord refinement adds a 1-cell *drawn inside* an existing 2-cell, so
`K_{P′}` is a **subdivision** of `K_P` and `|K_{P′}| ≅ |K_P|`. Triangulation changes nothing
topological. Verified by hand on AK(3) (triangle `aYX` glued to pentagon `axYXY` along `a`
reads back `xyxYXY`).

Also caught my own unit error: `minimum_defect` is `2·γ_N` in this repo. AK(3) is
`minimum_defect` 4 = γ_N 2 at rank 2 **and** at rank 9 — unchanged, as the theorem predicts,
not "2 → 4" as I first wrote.

## 06:53 UTC · Literature (A2): Lemma 11 identified, and the normal form is Lackenby's

Cloned `github.com/ammedmar/ac_paper` @ `d86984d`. **"Lemma 11" = Lemma 11 "Substitution and
Removal" of arXiv:2408.15332**, `sec/stable.tex:28–30` — exactly the free-definition move,
so it must be cited, not re-claimed; and its move cost is **unbounded** (the authors' own
open problem), not exponential as `FRAMING` trap 6 said. The triangular normal form is
**Lackenby's own Lemma 3.1**. Lackenby Thm 1.3 gives *unstable* AC-trivializability with no
rank restriction found in any channel. **The bounded-stabilization filtration `~^{(k)}` is
untouched in the literature** — that is where this line's novelty has to live. AK(3)'s
stable triviality confirmed OPEN from source.

## 06:56–07:01 UTC · Both bookkeeping mechanisms are dead

**S7** — change of variables is entirely a **depth-1** phenomenon (A1's Cor. F1 puts the
whole `Aut(Fₙ)` orbit inside `~^{(1)}`). Nine generators cannot buy a coordinate change that
three already buy. Consequence: the 261 unsolved MS representatives are **124 objects at
depth ≥ 1, and that is now a theorem, not a heuristic**.

**S8** — generator splitting (spread a crowded germ across copies) is **monotone**: 632
certified split states at ranks 4 and 6, 236 of them AK(3), **not one below its base**.
Proof sketch via `link(P)` being a **minor** of `link(P′)`; A3 independently confirmed the
minor relation edge-for-edge with a machine certificate.

**A1** proved Lemma S-a sharp as a biconditional, Cor. F1 at rank `n+1` with no fudge, and
found a **rank-8** triangulation of AK(3) by sharing a definition — so 9 is not minimal.

## 07:13 UTC · Audit of S3 — AMEND, and the one lead that works

1,525 independent triangulations, **zero counterexamples**, and a stronger theorem proved
(Lemma S3′: the *entire defect histogram* is preserved by a dart-level bijection). Repairs
applied: two missing non-degeneracy hypotheses; the predicate is **orientable PL**
thickenability; **T-S6 retracted** (it was founded on my unit error); T-S7's dividing line
corrected to *"two 2-cell germs from two **distinct** 2-cells"*.

**The lead (R7):** a degenerate-corner refinement *followed by free reduction* changed the
defect in 6 of 98 non-cyclically-reduced bases — **5 of them from defect 2 to defect 0**. The
refinement preserves the histogram; the *reduction* moves it. So high rank × **unreduced
spellings** is the live direction (task A10).

## 07:13 UTC · S10 — the first positive, and its control

Among presentations AC-trivial **by construction** (random AC walks from standard), at
matched total length 12–16, the orientably-thickenable fraction is **0.411 (rank 2), 0.726
(rank 3), 0.867 (rank 4)**. At length 15–24 **rank 2 is not computable at all** (80/80 blew
the census cap) while ranks 3–6 decide in seconds.

Distance control run: the absolute density **falls** with distance from standard (rank 2:
0.500 → 0.485 → 0.214), so the rates are near-standard and do **not** extrapolate to AK(3)'s
class; the rank **ordering** survives where samples are readable. S10 §5's AK(3) inference
was softened accordingly, from "strongest single piece of evidence" to "anomalously empty
relative to the one AC-trivial class measured the same way", with both confounds named and
no p-value quoted.

## 07:17 UTC · S12 — the method, and the honest answer to the brief

`S12_CERTIFICATE_HUNT.md`: **Stable Certificate Hunt** — stabilize `k` times, search AC1–AC3
at rank `n+k`, decide orientable thickenability at each state, cash any hit through Lackenby
Thm 1.3. Sound (proof in §2); a semi-algorithm; not a reduction to another open problem
because thickenability is decidable.

Corrected the audit's trap T-S9, which had the gap on the wrong side: an orientable PL
3-manifold *is* a 3-manifold, so **a hit is not encumbered by Joint-A at all**; the
orientability gap bites only on **nulls** — including the 0/171,842 depth-1 AK(3) harvest.

**The answer to the session's question, as it stands:** going to 9 or 10 generators does not
help *by any mechanism* — abbreviation is a provable no-op and splitting is monotone. It
helps by a **population effect** (certificates are commoner at higher rank) and a
**computability effect** (the decidable test is affordable at lengths where rank 2 is not).
That argues for the hunt at **rank 4–6**, not rank 2 and not rank 9.

> **Timestamp erratum.** The section headings above originally carried estimated times
> (07:0x, 07:4x, 08:0x). They are now the **measured** commit times from `git log`
> (`4cfd926`, `c3d36a0`, `a683cf3`/`0cd128c`, `1887488`, `9e330f3`). Estimating them was a
> direct violation of `experiments/lessons/cloud-session-network-and-push-constraints.md`
> ("measure log timestamps with `date -u`, never estimate") — filed here so the lesson
> keeps its teeth.

## 07:52–08:09 UTC · The audit lands, and the session's one positive claim dies

**A12 (adversarial audit) REFUTED S10.** At a fixed *total*-length band, rank and mean
relator length are the same axis (`mean = total / rank`). Holding **per-relator** length
fixed instead: 0.868 / 0.868 / 0.834 / 0.780 at ranks 2/3/4/5 — flat, then slightly
decreasing. My three headline numbers were the diagonal of a matrix whose real variable was
length. Both of my confound arguments died with it. S10 is kept with the retraction on top;
`contrast-length-confound.md` refiled as T-S10.

S12's step 3 repaired (Theorem D's `W` is compact, its double is closed orientable,
`N(K_Q) ≅ B³ ⊂ S³`) so all three readings of "thickenable" are discharged at once. S7 §5
corrected: the `aca_*` classes are ACA classes, not `Aut(F₂)` orbits, and **124 is an upper
bound**.

**A9 delivered a CERTIFIED negative and the session's one actionable find.** All 124
unsolved classes plus 67,864 class members are NOT_SPHERICAL under an exact procedure, with
an independent second solver agreeing 123/124 and failing closed on 1, Todd–Coxeter
confirming triviality on all 124, and a 54/55 positive ladder with zero false negatives.
Six certified **γ_N = 1 gateways** — `aca_117`, `aca_11`, `aca_121`, `aca_17`, `aca_30`,
`aca_122` — sit one unit closer to Lackenby's hypothesis than AK(3) (γ_N = 2). Verified
`aca_117` myself: 518,400-rotation census, defect 2, Todd–Coxeter index 1.

**New observation:** four of the six carry the same relator `YXXXyxx` = `y⁻¹x⁻³yx²`, the
Baumslag–Solitar relation `y⁻¹x³y = x²`, which appears in only 11 of the 124. Length-
controlled (≤ 18): 4 of 8 against 2 of 36. Recorded as a lead with its caveats, not a result.

**Also caught:** an earlier hunt loop I believed I had killed was still running 14 minutes
later on stale code, competing for the same four cores — filed as a postscript to the
search-instrumentation lesson (verify kills by PID; check `etime` against your last edit).

**Filed:** `stabilization-that-only-rebookkeeps-is-inert.md` and
`instrument-the-search-before-reading-its-null.md`, both indexed in `CLAUDE.md`.
**Synthesis:** `S13_SYNTHESIS.md`.

## Robustness of AK(3), as measured so far

Across ~2,500 distinct refinements, splits, shared definitions and spellings at ranks 3–9,
**AK(3) has never once dropped below `minimum_defect` 4 (γ_N = 2)**. Its depth-1 stable
harvest is 0 thickenable in 171,842 members while the AC-trivial AK(2)+z harvest under the
same operator is 54.8 %. Bounded, confounded, and pointing at the disproof side — not a
disproof.

## Running at time of writing

A6 cubic normal form · A7 rank-`N` search + depth ladder · A8 move classification ·
A9 thickenability sweep of the 124 unsolved classes · A10 spelling × high rank.

---

## 09:00–09:25 UTC — THE HEADLINE IS RETRACTED, and what replaced it is better

**S13 §3z-bis, labelled "the strongest result of the session", is retracted in full**
(`S16_CONTROL_RETRACTION.md`, trap **T-S19**).

The calibration control `("XYXXY","XXYXYXXY")` was AC-trivial, rank-matched, length-matched
and pipeline-matched — and, unnoticed, **already `SPHERICAL` (`γ_N = 0`) itself**. So it
measured *survival* of a certificate, not *creation* of one. Replaying all 759 of its hits
gives the defect chain `(0,0,0,0)` × 759 with **zero** chains that left 0 and came back.
Widening to five AC-trivial rank-2 sources of the **same total length 13** that are **not**
thickenable: `0` hits in 61,157 across four such sources — exactly AK(3)'s score. Source `γ_N` values recomputed in
this clone with the repo's own decider.

So **AK(3)'s 0/45,111 is not a fact about AK(3)**, and "expected ≈ 681" is void with the
rate that produced it. The tell was in the retracted section's own words — *"only the
source's rank-2 defect differs"* — the one unmatched axis was the property being measured,
named directly above the conclusion that attributed the gap elsewhere.

**What replaces it, and is worth more — a route retired with a mechanism, not with silence:**

| measurement | rank | creations | opportunities |
|---|---|---|---|
| sharp control, 4 non-thickenable sources | 12/13 | 0 | 61,157 |
| AK(3) pool | 12/13 | 0 | 45,111 |
| **total, states from 5 non-thickenable roots** | | **0** | **106,268** |
| flip census (separate experiment) | 5 | 0 | 1,470 |

(The 759 replayed control chains are **not** in that denominator — they start at defect 0,
so they are not opportunities to *create* one. Pooling them gave an inflated 93,638 in the
first draft; caught by the S13 audit and corrected everywhere.)

The chord + SPLIT pipeline is certificate-**preserving** and certificate-**non-creating**,
while demonstrably not blind (759 certificates found whenever the root already had one).
It can only settle a presentation that already has a thickenable member at rank 2. AK(3)
does not. **The cubic route cannot settle AK(3)** — and this is a mechanism, so it holds for
every future target, not just this one. The rank-13 cubic normal form itself is untouched.

Also measured: AK(3)'s defect 4 never appeared among 33 random AC-trivial length-13 rank-2
presentations (`{0: 20, 2: 13}`), so no exactly-matched control can be built.

**Theory written alongside** (`S15_ONE_SIDEDNESS.md`):
- `γ_N` is **not** a stable-AC invariant — witness `AK(3)` (`γ_N = 2`) `~_st` `C1`
  (`γ_N = 1`). This kills the cleanest disproof idea on the line. Bracket unchanged:
  `0 ≤ Γ(AK(3)) ≤ 1`, and the upper bound 1 was already available at rank 2.
- The route is **one-sided**: Lackenby Thm 1.3 is an implication, so every instrument here
  bounds `Γ` from **above**. A null bounds nothing below. An uncalibrated null on a
  one-sided route carries zero information.
- **Stable AC ⟺ every balanced trivial-group presentation has a thickenable member in its
  stable class** (conditional on Thm 1.3, which `literature/` does not contain — only
  `literature/fake_surfaces/` is present, checked by `ls` while writing). So the brief's
  "simple general way" *would be* the conjecture, not a step toward it.
- No lower-bound tool can be a homotopy invariant: every balanced trivial-group
  presentation complex is contractible (not new — `R10` L1).

**High-budget gateway sweep, 8× nodes.** Control detection by target length:
`aca_11` (L15) 12/12 · `aca_121` (L16) 8/12 · `aca_17` (L17) 7/12 · `aca_122` (L18) **0/12**.
Target 0/8 everywhere. The length-18 null is not weak evidence, it is *no* evidence.

## Branch facts the user needs

- **`fable/proof` does not exist on the remote.** `git ls-remote --heads origin` lists no
  such branch, so "merge this into `fable/proof`" needs the user to create it (or pick a
  different target). Everything from this session is on
  `claude/stable-ac-conjecture-stabilization-rwo9as`, pushed.
- The sibling solver's branch is **`codex/proofs`** (plural), not `codex/proof`. It has an
  **unrelated git history** to this branch — no shared root, so no collision risk. Its five
  most recent commits are streaming / semantic-checkpoint / push-log infrastructure, not AC
  theory, so the two lines remain complementary. Checked 09:24 UTC.

---

## 09:25–09:47 UTC — the comparison redone twice more, and it finally holds

**Third design of the target-vs-control comparison.** The first two are retracted; each
failure told me what to fix.

1. §3z-bis — control already `γ_N = 0`: measured survival, not creation (T-S19).
2. `s12` hunt at length 13 — controls verified **not** thickenable, but at **defect 2**
   (a one-unit descent) while AK(3) is at **defect 4** (two units). Matched on every axis
   except the one that mattered, for the second time.
3. **S21** — controls matched on defect 4 as well. Found by scanning 240 solved-ladder
   members and reading the defect off the *stabilized* form: 78 with a defect number,
   73 at defect 2, **five at defect 4**. The match was established *before* the result was
   read.

| matched defect-4 control | len | trials | hits |
|---|---|---|---|
| `('YYXyx','YYxxxyXX')` | 13 | 8 | 7 |
| `('YYXyx','YYxxyXXX')` | 13 | 8 | 4 |
| `('YYXyx','YXYXXyxx')` | 13 | 8 | 8 |
| `('YYXyx','YXXYxxx')` | 12 | 8 | 8 |
| `('YYXyx','YXXXYxx')` | 12 | 8 | 8 |
| **pooled** | | **40** | **35 = 0.875** |

AK(3), same instrument and budget, three independent runs (seeds 811/812/907):
**0/16 + 0/12 + 0/6 = 0/34**, with those runs' own controls at 15/16, 12/12, 9/9.

Four caveats, in `S21_MATCHED_NEGATIVE.md` §5. The load-bearing one is **§5.2**: AC-distance
to the trivial presentation is unmatched and **cannot** be matched — any known-AC-trivial
control is close to triviality in a way AK(3) is not known to be. No choice of control
removes it. Also: all five controls share the relator `YYXyx`, so effective independence is
below five; and this bounds `Γ(AK(3))` from **above** only. Under adversarial audit (A21),
whose first instruction is to find the *fourth* costume of the trap.

**Machine-confirmed on the way, and it answers the brief's central hypothesis directly:**

| | rank 2 | rank 3 | rank 4 | rank 5 |
|---|---|---|---|---|
| AK(3) | NOT_SPHERICAL | defect 4 | defect 4 | defect 4 |
| two controls | NOT_SPHERICAL | defect 2 | defect 2 | defect 2 |

**Stabilization is inert for `γ_N`.** Adding generators alone does not move it at any rank —
AK(3) is still at `γ_N = 2` at rank 5 and would be at rank 20. The presentation does not get
easier by being stabilized; only moves that *use* the new generators can help, which is why
A6's length-3 SPLIT (`γ_N` 2 → 1) was the one mechanism found all session that moved it.
It also means a control verified non-thickenable at rank 2 remains a valid *creation* control
at every depth of a ladder.

**The audit of S13 (A15) cost four corrections**, one arithmetic: the creation denominator
was 93,638, but the 759 replayed control chains *start* at defect 0 and are not opportunities
to create anything. With the fourth control source now decided the figure is
**0 in 106,268** from five non-thickenable roots. A15 also refuted a sentence of mine in S15
— "no known move manufactures one" is false; a plain AC1–AC5 search created `γ_N = 0` from a
non-thickenable length-13 base in **39 of 40** runs. It is the *cubic* pipeline specifically
that never manufactures one.

---

## 09:50–10:22 UTC — the third headline falls too, and the session's answer settles

**`S21` is retracted (T-S20).** A18's audit of the sibling S5 ladder found the confound and
my own data confirmed it within minutes: every control hit is a walk *down* in length —
witnesses at 3, 3, 4, 4, 5, 7, 7, 8, 9, 9, 10, 11, 11 from a start of 13 — into a region
AK(3) provably cannot enter, since any member of its class below length 13 would settle the
open problem (Havas–Ramsay). Restricted to length-13 targets, requiring only that the witness
end back in band, the rate falls **19/24 = 0.79 → 8/24 = 0.33**, and A18's freshly built
defect-matched control scores **0 of 8 in band**, with **0/32 at rank ceilings 3–6** — extra
rank actively *hurt*.

**A21's independent audit reached RETRACT from the opposite direction, and made the lesson
stronger.** Told to hunt for a *structural* fourth confound, it found four candidates (shared
`YYXyx`; 5+8 vs AK(3)'s 6+7; every control having a relator abelianising to a generator, which
all 640 members of `ms640_solved.txt` do; the unprinted rank-2 defect) — then built controls
with **none** of them, including one matched on the 6+7 shape, and the rate did **not**
collapse: 8/8, 8/8, 8/8, pooled 59/64 across two unrelated sources. So there is no structural
fix. It also replayed a hit end to end:

    length trace 13,12,15,18,17,11,14,17,10,11,15,17,14,9,8,7 — certificate created at 7

every step verified a legal AC move, `minimum_defect = 0` by exact census. A perfect hit,
obtained through an exit the target is denied.

**Two errors of mine it caught.** `0/34` was quoted from console output while two of its three
runs were *still executing* (`s12_hunt` writes JSON only after its last depth) — the
artifact-backed figure is **0/16**. And all five control runs shared `--seed 991`, so "40
independent restarts" was 8 streams reused 5 times. Both folded into T-S20.

**`S20`: the planarity obstruction is PROVED but is not the mechanism.** Non-planar link ⇒
`γ_N ≥ 1` (falsification test: 945 `γ_N = 0` states, 945 planar, 0 non-planar) — the line's
first certified *lower* bound. But it bounds a **state's** `γ_N`, not `Γ`; non-planarity is
**not** preserved by SPLIT (certified `K3,3` → verified sphere counterexample); AK(3)'s link
is exactly `K4` so it is vacuous at rank 2; and only 16.9 % of `γ_N = 1` parents are
non-planar while 47.5 % of the 56,388 edges have a *planar* child. It explains at most half
the empty `1 → 0` cell. My hypothesis is dead, and it died with a proof and a counterexample.

**Instrument limit, and it is the binding one for the brief.** Both depth ladders spent
**> 45 minutes inside depth 1 without completing it** (2,000 and 8,000 nodes), and `s12_hunt`
writes its JSON only at the end, so an interrupted run yields **no artifact at all**. The
brief's regime — depth ≥ 1, rank ≫ 3 — is not testable with this tool at this budget.
High-rank hunting needs incremental persistence and a cheaper decider first.

**Test suite: 787 passed, 8 skipped, 0 failed** (346 s), so nothing in the repo was broken —
every instrument this session was a new file.

**Final synthesis: `S22_FINAL_ANSWER.md`**, superseding `S13` §1.
