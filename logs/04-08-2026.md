# 04-08-2026

## 23:33:00 UTC · `2e395d85`

Exported the μ-ladder's 36-of-124 descent census as a per-class annotation of `aca_124.csv`: [`data/ms_unsolved_reps/aca_124_reduced.csv`](../data/ms_unsolved_reps/aca_124_reduced.csv) adds `reduced`/`mu_in`/`mu_out`/`new_r1`/`new_r2`/`n_hops`, with the literal string `none` on the 88 classes that never descended, written by [`experiments/stable_ac/cov/ladder/export_aca124_reduced.py`](../experiments/stable_ac/cov/ladder/export_aca124_reduced.py) from `mu_ladder_big_aca124_r256_b64_mrl24.jsonl` — all 36 reduced rows re-verified against the pure-Python `aut_canon` (each recorded pair *is* its own Aut-canonical rep at the recorded total, so this is also a fast-vs-slow cross-check of `autcanon_fast` on the full descender set).

Also scored an external length-reduction table (22 MS presentations) against that census with [`experiments/stable_ac/cov/ladder/compare_external_reductions.py`](../experiments/stable_ac/cov/ladder/compare_external_reductions.py) → [`results/stable_ac/mu_scan/external_compare.csv`](../results/stable_ac/mu_scan/external_compare.csv). Matched on the MS **label** against `members` (never a row index) and re-verified label→pair against `ms_reps_unsolved.csv`, 22/22 exact. 19 of the 22 classes are in our 36; head-to-head on those the ladder floor is lower on 9 and ties on 10, never worse. Three external rows drop raw length without moving the Aut-orbit at all (`d_mu = 0`) — raw total length is not the ruler, the orbit floor is. `pytest tests/stable_ac -q` green (311 passed, 1 skipped); [`data/README.md`](../data/README.md) gains the new file's row.

## 23:50:22 UTC · `8a3ec0ad`

Folded the external table's 3 non-overlapping classes into [`data/ms_unsolved_reps/aca_124_reduced.csv`](../data/ms_unsolved_reps/aca_124_reduced.csv) via a new `--ext` flag on [`export_aca124_reduced.py`](../experiments/stable_ac/cov/ladder/export_aca124_reduced.py), taking the file to **39 reduced / 85 not** — but the two claims are not the same claim, so `reduce_kind` now carries the criterion and `reduced` alone is no longer sufficient to read the file. 36 rows are `mu_floor` (`mu_out < mu_in`, a strictly lower Aut-orbit floor, each re-verified against pure-Python `aut_canon`); 3 are `length_only` — `aca_2` (17_41), `aca_14` (16_7), `aca_53` (19_33) — where the external finish shortens a member's raw spelling but canonicalises to `mu_out == mu_in`, zero orbit distance. New columns: `reduce_kind`, `source`, `ext_label`.

The `length_only` branch is guarded, not assumed: if an external finish ever canonicalises **below** the class's `mu_in` it aborts rather than filing a real orbit descent under the weaker label. Also recorded that all 124 class reps are already Aut-canonical at `mu_in`, so the raw-length and orbit-floor rulers agree at the start and can only diverge at the finish — which is exactly where these 3 diverge. [`data/README.md`](../data/README.md) row rewritten to lead with `reduce_kind`.

Added the drop-in twin [`data/ms_unsolved_reps/aca_124_best.csv`](../data/ms_unsolved_reps/aca_124_best.csv) (`--best-out`, regenerated from the same pass so the two files cannot drift): `aca_124.csv`'s exact five columns, `r1`/`r2` swapped to the reduced pair on the 39 reduced classes and byte-identical on the other 85, 2446 → 2356 total letters. It deliberately carries **no** reduction bookkeeping — anything that loads `aca_124.csv` loads it unchanged — so `aca_124_reduced.csv` stays the only place that says which rows moved and on which criterion. Checked row-for-row against `aca_124.csv`: schema identical, membership columns untouched, every unreduced row byte-equal, every reduced row equal to its `new_r1`/`new_r2`.
