# 10-08-2026

## 19:20:00 UTC · `32bc4a6a`

Shipped the abelian-mass **top-3 CoV** rule over all 640 ms640 presentations at budget 100,000, as a two-stage pipeline sharded across two parallel Colab sessions. Stage A enumerates each presentation's subword-CoV family, ranks it by `abel(r1,r2) = |σx(r1)|+|σy(r1)|+|σx(r2)|+|σy(r2)|` and freezes the top 3 — **1,920 picks, zero search nodes, ~2 s**; the selection has to be frozen on disk before any search runs, or two parallel sessions can disagree about what they are searching. Stage B searches those starts in rank order and **stops at the first solve**, so a presentation costs at most 3 × 100,000 = 300,000 nodes, and records `cum_nodes` per row. The key and its tie-break are imported from `abel_topk_cov_b1k`, never re-implemented, and the tests pin that Stage A's independently-enumerated ranking reproduces the validated budget-1,000 selection on all 60 subset-60 rows — the ms640 run measures the same rule the subset-60 result reports. *(The modules, doc, tests and results directory of this section were renamed in the 21:05 section below; the links there are the live ones.)*

The node-matched control costs zero new search: a greedy search at budget `B` is exactly the first `B` pops of a longer one, so plain greedy at 300,000 is the frozen `greedy_1000000_640_*.jsonl` read with `solved and nodes_explored <= 300000` → **638/640, unsolved {634, 635}** (one 100,000-node search alone is already 634/640). Which means ms640 is nearly saturated at this budget and a solve-count headline has almost no room in it — the [control-with-no-dynamic-range](../experiments/lessons/control-with-no-dynamic-range.md) shape. The frozen 10,000-node sweep settles four of the six movable rows for free: abel top-3 takes **634 at rank 1 in 7,840 nodes and 635 at rank 1 in 7,875** — exactly the two the control cannot reach, a ~38× cost win — while no CoV in the family solves 636–639. So the dimension with real dynamic range here is **cost**, and 636–639 are the only genuinely open headline rows.

Also landing with its runner: [`experiments/heuristic_search/runners/abel_topk_cov_b1k.py`](../experiments/heuristic_search/runners/abel_topk_cov_b1k.py) + [`tests/heuristic_search/test_abel_topk_cov_b1k.py`](../tests/heuristic_search/test_abel_topk_cov_b1k.py) and its subset-60 writeup [`results/comparison/ABEL_TOPK_COV_B1K.md`](../results/comparison/ABEL_TOPK_COV_B1K.md). Gates: `pytest tests/greedy tests/stable_ac tests/analysis -q --runslow` → **1123 passed, 239 skipped**.

## 21:05:00 UTC · `5107b9e8`

Re-cut the ms640 census as a **two-rule comparison**, one Colab session per rule, each over all 640 presentations, on the user's instruction: the deliverable is `nodes_explored` and `path_length` against plain greedy, not another solve count. `abel` (abelianized magnitude) and `len` (shortest transformed pair, `KEYS["len_only"]`) are both imported from `abel_topk_cov_b1k` — and they are not two names for one thing: the two rules pick the same top-3 **set** on only 5 of the 640 presentations. Sharding by rule replaces sharding by row (`CHUNKS` stays, defaulted to 1, for splitting one arm further). Renamed accordingly, nothing having run yet: `abel_top3_manifest.py`/`abel_top3_run.py` → [`cov_top3_manifest.py`](../experiments/stable_ac/cov/run/cov_top3_manifest.py)/[`cov_top3_run.py`](../experiments/stable_ac/cov/run/cov_top3_run.py), `ABEL_TOP3_MS640.md` → [`COV_TOP3_MS640.md`](../experiments/stable_ac/cov/run/COV_TOP3_MS640.md), `tests/stable_ac/test_abel_top3.py` → [`test_cov_top3.py`](../tests/stable_ac/test_cov_top3.py), `results/stable_ac/cov/abel_top3/` → `results/stable_ac/cov/cov_top3/`, and the two chunk notebooks → [`cov_top3_ms640_abel.ipynb`](../experiments/notebooks/stable_ac/cov_top3_ms640_abel.ipynb) + [`cov_top3_ms640_len.ipynb`](../experiments/notebooks/stable_ac/cov_top3_ms640_len.ipynb) (`RULE` is the only knob that differs). The abel manifest's selection is byte-identical to the one committed at 19:20 — only the new `rule` field was added.

**The rule↔manifest binding is enforced, not documented**, because with two manifests on disk a stale path plus the other rule's name is a wrong experiment wearing a plausible filename and nothing in the results could reveal it: the manifest path is *derived* from the rule, every manifest row carries its `rule`, the loader raises on a mismatch, and the rule is the first field of `_run_prefix` so the arms can never resume into each other. Four new tests cover it, including an end-to-end `run(rule=other)` refusal.

Rows now carry everything the comparison needs with no join — the search (`solved`, `nodes_explored`, `path_length`, the `path_moves` tuples `verify_results` replays), the pick, the presentation's running `cum_nodes`, **and** the untransformed route's own numbers for the same presentation (`base_solved`, `base_nodes_explored`, `base_path_length`, read once from the frozen 1M baseline). `summarize()` leads on the paired nodes/path comparison over the presentations *both* arms solved — median/mean/max plus win/tie/loss on each — after the solve counts, and prints that a CoV path certifies the transformed pair, never the original. `compare_rules()` scores the two arms on their intersection.

Smoked by exec'ing the real CONFIG/SETUP/RUN cells of both notebooks at budget 1,000 over 20 stride-sampled presentations (`CHUNKS=32`), then the head-to-head cell: abel 20/20 and len 19/20 against plain greedy's 17/20 at one search and 19/20 node-matched, with abel mean 66 nodes / mean path 16.9 against plain greedy's 547 / 24.9 on the same rows — the shape the full run is expected to have. `verify_results` had never been fed a row of this format, so a 5-presentation file per rule was generated at budget 1,000 and run through it before the user spends core-hours: **5/5 certificates verify on each arm, exit 0** — the new `mode` strings and the `base_*` fields do not confuse the verifier, and the "Afterwards" step in the method doc is now a checked instruction rather than an assumed one. Gates: `test_cov_top3.py` 50 passed; `pytest tests/greedy tests/stable_ac tests/analysis -q --runslow` → **1149 passed, 239 skipped in 228 s**.

## 22:40:00 UTC · `176a8553`

**Dropped the early exit: every presentation now searches all 3 ranks**, on the user's instruction ("run all top 3 even if the first solves since we want a genuine mean"), and they are right that it is the better experiment. Under early exit ranks 2 and 3 would have been searched only where rank 1 failed — a harder, self-selected subset — so their means would have been worse than rank 1's *by construction* and the per-rank comparison would have been circular. Running every rank measures all three on the same 640 presentations, which is the only direct evidence that the ranking rule orders the CoV family by quality at all. The 20-presentation dry run already makes the point: abel r1/r2/r3 solve **20/20 each** at mean **66/55/55** nodes and mean path **16.9/16.5/16.6** — rank 1 is not better than rank 3 on easy rows, a fact early exit would have hidden by never searching r2 and r3 there.

Nothing is lost in the other direction, because the ranks run in order and every row already carried its running `cum_nodes`: the deployed cost is recovered exactly as `first_solve_nodes` (ranks 1..r, r = first solving rank). A presentation now has **two costs that must not be confused** — `first_solve_nodes`, what the rule would cost deployed as a solver, and `cum_nodes`, what the census spent — and `summarize()` pairs the *first-solve* cost against plain greedy, because plain greedy's number is one search that stopped when it solved, so charging the CoV arm for ranks a user would never run would compare a census against a solver. The census total is printed separately, beside a new per-rank block (solved, median/mean nodes, median/mean path, per rank).

The half of this that fails silently is resume: `_finished` now requires **all** k ranks, and a solve does not finish a presentation. Had a solved rank 1 kept counting as finished, a Restart → Run All would have skipped precisely the presentations rank 1 solved, the file would have looked complete, and ranks 2-3 would have been measured on the complement of the easy rows — the same self-selection, reintroduced through the resume path. `test_restart_fills_in_the_ranks_after_a_solve` pins it by truncating a finished file to its rank-1 rows and requiring the resume to fill the rest in; both new tests assert their fixture actually exercises the case rather than passing vacuously. The reporting half had the same defect and was caught on review: `summarize()`'s `partial` list still read "a solve finishes it" (`not solved and n_searches < k`), so mid-run a presentation solved at rank 1 with ranks 2-3 outstanding would have been reported as nothing left to do while the per-rank block printed over unequal sets. It is now `n_searches < k` alone, mutation-checked (the old predicate fails the extended test on both rules). The verifier was re-run on the *new* file shape too, since the 5-presentation check at 21:05 was made against early-exit output that no longer exists: 5 presentations per rule at budget 1,000, 15 rows each of which 10 come after an earlier rank already solved — **30/30 certificates verify, exit 0**.

Cost: every presentation is now exactly 3 searches, and the bill is dominated by ranks that never solve and burn the full 100,000. On the subset-60 overlap — the hard tail, so a pessimistic rate — **17.2% of abel's picks and 21.1% of len's are still unsolved at 10,000 nodes**; the bound is 640 × 3 × 100,000 = 192M nodes per arm and the realistic figure is well under that, but it is several times the early-exit run and the user should expect hours, not an hour. Gates: `test_cov_top3.py` 52 passed; `pytest tests/greedy tests/stable_ac tests/analysis -q --runslow` → **1151 passed, 239 skipped in 235 s**.

## 23:15:00 UTC · `732c8b2e`

**Both ms640 arms are in, and the abelian-mass rule takes the whole dataset: 640/640.** Full results, with every number regenerable from the two jsonl: [`results/stable_ac/cov/cov_top3/RESULTS.md`](../results/stable_ac/cov/cov_top3/RESULTS.md). The node-matched control (plain greedy at 3 × 100,000, by truncation) takes 638, `len` takes 638, and abel is the only arm that takes 634 and 635 — which it does at rank 1 for under 8,000 nodes each. Paired against plain greedy's own cost over all 640: median 9 vs 11, mean **717 vs 4,963**, max 26,963 vs 574,959, cheaper on 511 / tied 49 / dearer on 80; paths median 8 vs 9. Head-to-head on the 638 both rules solve, abel costs 694 nodes to `len`'s 1,758. All 3,813 solved rows of the two files verify, exit 0, and the repo-wide standing count in [`results/README.md`](../results/README.md) is now `ALL 13649 SOLVED-ROW CERTIFICATES VERIFY (45633 rows across 14 files)`.

The solve count was pre-registered as the metric with no room in it, and that reading survives for the middle of the dataset — but the six rows plain greedy cannot take at 100,000 all fall to abel's **rank-1** pick, at 11×–73× less than plain greedy's own cost at the 1,000,000-node budget where it does succeed (the [price-the-untransformed-route](../experiments/lessons/price-the-untransformed-route.md) comparison, not the budget where it was last seen to fail). The method doc's "no candidate in the whole CoV family solves 636–639" was a budget-10,000 statement and is marked superseded in place: all four solve at 14,470–19,340 nodes, just out of that sweep's reach. Cost is also reported stratified on the *control's* own difficulty, because the pooled mean is tail-dominated — abel's median wins in all five bands and its mean crosses over at 1,000 nodes (2.7×, then 9.0× above 10,000); the 80 presentations where it loses cost **70,929 excess nodes in total, median excess 4**, and 60 of them under 100.

Running every rank below a solve is what paid off here, and it paid in the direction the design argued: with all three ranks on all 640 rows, abel is monotone (640/635/633 solved, 717/1,486/1,769 mean nodes) while **`len` is inverted** — 633/635/637 solved, 2,042/1,609/1,262 nodes, so its rank 3 beats its rank 1 and its rank 1 alone is *below* plain greedy's 634 at a single search. Shortest-transformed-pair is not a weaker key than abelian mass, it is anti-correlated with quality as a primary key. Abel's rank 1 alone being 640/640 also settles the policy: **deploy k = 1** at 458,688 nodes, against the census's 2,541,652. The abel file's provenance shows the resume fix working in production — its 640 rank-1 rows carry `eec965fa` (early-exit code) and its 1,280 rank-2/3 rows `ebf4d76f`, i.e. the restart filled in behind the all-ranks change without re-searching rank 1, exactly what `test_restart_fills_in_the_ranks_after_a_solve` pins.

On the tie-break, the user is right that ranking ties on the z-word's spelling is arbitrary, and the arms now price the alternative at zero search cost. Abelian mass is a **filter, not a ranking**: it leaves a median of 5 candidates tied and collapses to one on only 86/640, so on 462 of 640 rows all three picks share an abel value and the tie-break chooses. Total transformed length is the right second key — on the 431 abel-flat families whose picks differ in length and all solved, a shortest pick was also a cheapest one on **398 (92%)** against ~33% by chance, and re-scoring the already-searched top 3 under `(abel, length, lex)` costs **420,419 nodes instead of 458,688** (better on 81, worse on 27, tied on 532). The canonical form, though, belongs as a **dedup and not as a third sort key**: two candidates differing only by a cyclic rotation are the same state to the solver, and all **150** within-presentation groups of Booth-canonically identical picks across both arms returned bit-identical `(solved, nodes_explored, path_length)` — as a sort key their keys are *equal*, so both still enter the top 3, whereas deduping before the top k recovers a wasted search on the 112/640 rows whose `(abel, length, lex)` top 3 contains such a pair. `len` already spent 325,963 nodes (10% of its census) on 126 such repeats; abel spent 707. Exact `(r1, r2)` duplicates are zero in both, so this is only visible under canonicalisation. Not implemented — it is a new rule and a new arm, and the re-score above bounds only the reordering, not the full rule.

Deck: [`+/website/week_8/`](../../+/website/week_8/) gains three slides (the census, the six hard rows, the rank ordering) and three figures, all generated — `make_figures.py` now reads the two production jsonl through the top-3 runner's own `per_presentation`/`baseline_at`, so `stats.js` and `RESULTS.md` cannot disagree, and `summarize()` is deliberately not called because it routes through `load_config`'s local budget guard. Gates: `pytest tests/greedy tests/stable_ac tests/analysis -q --runslow` → **1151 passed, 239 skipped in 289 s**; `verify_results` over all of `results/stable_ac` → exit 0.

## 23:40:00 UTC · `d527bd14`

Priced the user's proposed tie-break — abelian mass, then the shorter relator, then length, against abelian mass, then length, then the shorter relator — on subset-60 at budget 1,000, top 3, at **zero search nodes**: [`experiments/heuristic_search/runners/abel_tiebreak_b1k.py`](../experiments/heuristic_search/runners/abel_tiebreak_b1k.py) re-ranks the frozen `covsweep_1000_66_*.jsonl` through `abel_topk_cov_b1k`'s own gated loader, so the candidate set, the anti-leak gate and the 6,788-row truncation gate are the ones already carrying the incumbent numbers. Writeup: [`results/comparison/ABEL_TIEBREAK_B1K.md`](../results/comparison/ABEL_TIEBREAK_B1K.md) + `abel_tiebreak_b1k_subset60.csv`.

**The proposal as worded is one arm, not two.** Every candidate is a two-relator pair, so mean relator length = total / 2 — a strictly monotone function of the total and therefore the identical ordering; `assert_mean_is_length` proves the two produce the same top 3 on all 60 rather than arguing it. The substantive contrast is `min(|r1|,|r2|)`, and it is a **dead heat** with the total: same 41/60 at k=3, same 40/60 at k=1, differing on 1 of the 41 both-solved presentations by 2 nodes in 4,631 (0.04%), and identically 1 row / 2 nodes when the same arms are re-scored on the 10,000-node twin of the sweep. Min carries no signal the total does not — as a sole second key it collapses to a unique pick on 24/60 against the total's 31/60, and bolting it on after the total moves the whole bill by 4 nodes in 61,627. Note also that the "min relator" intuition was earned by `min_relator_length` as a *search progress* signal, whereas this key reads `min(len(r1), len(r2))` off the start pair; the anti-leak gate exists so no key can reach the search-derived column.

**What the run does separate is the incumbent's third key.** `abel_len_lex`'s `longest` costs one rank-1 pick (39/60 against 40) and 1,000 nodes at k=3 — a single presentation, 533, whose rank-1 under `longest` burns the whole budget. Both `total` and `min` avoid it. And the honest scope note: that +2-row rank-1 margin over bare `abel` at budget 1,000 **inverts at 10,000**, where bare `abel` reaches 52/60 at rank 1 and every length-keyed arm reaches 51 with a higher mean. Subset-60 cannot separate these keys at all — the oracle is 45/60 at 1,000, every abel-first arm sits on the same 41, and one or two rows is the entire dynamic range ([control-with-no-dynamic-range](../experiments/lessons/control-with-no-dynamic-range.md)). The powered evidence for `(abel, total)` remains the ms640 census. The genuinely held-out set cannot help either: **0 of the 124 unsolved representatives has any solving CoV candidate at budget 50,000**, so every arm ties at zero there.

After `abel`, `min` and `total`, 26 of 60 presentations still need a further key, and with two relators `max = total − min` leaves no length feature to spend — which is the residue the ms640 finding already claims, as a **Booth-canonical dedup before the top k**, never as a fourth sort key. No dedup is applied here, deliberately, so the numbers stay comparable to the incumbent 41/60. Gates: `pytest tests/heuristic_search -q` → 97 passed, 2 skipped (excluding the untracked, never-committed `test_fftvar.py`, which imports a `core/search_signlag.py` that has never existed on this branch); `pytest tests/greedy tests/stable_ac tests/analysis -q --runslow` → **1151 passed, 239 skipped in 512 s**.

## 23:51:00 UTC · `3c6749f2`

Corrected the tie-break study: the "min" the user meant is **`S`**, `hlab.FEATURES[7]` — the *smallest mean block*, the mean run length of the thinner generator read cyclically over both relators — not `min(|r1|,|r2|)`. `S` carries the heaviest weight in [`hsolve.RECOMMENDED`](../experiments/heuristic_search/core/hsolve.py) (8.458 against `L`'s 1.0), which is exactly why it was worth asking about. [`abel_tiebreak_b1k.py`](../experiments/heuristic_search/runners/abel_tiebreak_b1k.py) now ranks on it, still at **zero search nodes**, and the answer is not the dead heat `Lmin` gave: **total length first wins.** `(abel, total, S)` takes 40/60 at rank 1 against `(abel, S, total)`'s 38, at a paired mean of 113.0 nodes against 166.1 over the 41 both solve — 5 rows dearer, 0 cheaper, 36 tied, and two of the five losses are a rank-1 pick that burns the entire budget. `S` behind length is inert (6 nodes in 61,627); `S` alone, with `abel` dropped, is 35/60.

Because a key costs nothing to evaluate, `second_key_sweep` now scores **all 17 features in both positions** plus `reco` (the full RECOMMENDED linear score as one key). The result is one-sided: not one feature placed ahead of total length beats it — 9 of 18 lose rank-1 solves, and `Bmax`/`xyimb`/`Bmaxrun`/`Bspread` collapse from 40/60 to **22/60** and take the bill from 61,627 to 83,317 nodes. Those four are precisely the features that discriminate *most* (55–58 of 60 decided outright), which is the finding worth keeping: **a key that breaks more ties is not a better key, it is a key that overrides length more often.** The same shape appears in the discrimination table — `abel + S + total` reaches 48/60 unique picks, more than any other chain, and is one of the worst arms in the file. Placed behind length, 12 of 18 land within 6 nodes of `(abel, total)` and the other 6 give a rank-1 solve back; nothing improves on `(abel, total length)` measurably.

One near-miss is priced rather than reported: at budget 10,000 `abel_len_S` posts the lowest bill of any arm (294,036 against `(abel, total)`'s 304,030) and is the only length-keyed arm at 52/60 rank 1 — but paired that is **win/tie/loss 1/50/1**, one presentation (634) worth the whole budget, and at 1,000 the same pair runs 0/40/1 the other way. A margin carried by one row on a 60-row set is the repo's own [gap-metric](../experiments/lessons/gap-metric-saturates-when-the-treatment-wins.md) shape. Also new: `assert_lmin_is_minrel` pins this file's `_minrel` against `FEATURES["Lmin"]` so the arms named `min` and the sweep row named `Lmin` cannot drift apart, and the vacuous `assert_search_free` (`min + max == total` is an identity, so it could never fire — [cap-monotonicity-vacuous-guard](../experiments/lessons/cap-monotonicity-vacuous-guard.md)) was deleted; the real anti-leak gate is `load()`'s `leaks == 0`.

**The mechanism, and the correction it forces.** `S` never ships alone — it ships inside `L + 20*S + 2*MK` (`s20_mk2`) and inside `RECOMMENDED`'s weighted sum, **always with `L` in the same expression, where `L` can outvote it**. A lexicographic first key cannot be outvoted by anything. Scoring the composite itself settles it: `(abel, L + 20*S + 2*MK)` is the joint-cheapest arm in the file at 61,625 nodes and 40/60 at rank 1, against `(abel, total)`'s 61,627 and 40/60 — a 2-node difference. The same `S`, lexicographically ahead of length, is 38/60 and 63,812. So the "before length" column is **not** evidence that these features are bad; it is evidence that lexicographic priority is the wrong way to spend them, and no arm in `hsearch`/`hsolve` has ever used one that way. The magnitudes agree: `L` runs 6–57 over the 6,177 candidates while `20*S + 2*MK` sits in a band of sd 5.1 — enough to reorder similar-length pairs, never enough to put a long pair ahead of a short one. Two limits kept on the record: the composite's 10,000-node edge is win/tie/loss 2/50/0, one presentation (634), and with `abel` dropped `s20_mk2` alone ranks *worse* than length alone (36/60 against 39 at 1,000).

Standing caveat carried forward, unchanged: these are **start** features, read off the pair before a node is popped, while `S` and `Lmin` earn their weights as **climb** features re-evaluated at every node. Nothing here is evidence about the heap ordering. Gates: `pytest tests/heuristic_search -q` → **97 passed, 2 skipped** (excluding the untracked `test_fftvar.py`, which imports a `core/search_signlag.py` that has never existed on this branch).

## 00:12:00 UTC · `PENDING`

Answered the question the previous section deferred — *what breaks the tie after `abel` and total length?* — and the answer is **nothing, and the previous section's recommendation was wrong to offer the canonical dedup as headroom.** Three measurements in [`abel_tiebreak_b1k.py`](../experiments/heuristic_search/runners/abel_tiebreak_b1k.py) (`tie_residue`, `dedup_rank`, `solve_rank_curve`), still at zero search nodes, all in [`ABEL_TIEBREAK_B1K.md`](../results/comparison/ABEL_TIEBREAK_B1K.md).

**Half the tie is not a tie.** The 102 candidates tied at `(abel, total)`'s minimum across the 29 still-tied presentations are only **52 distinct Booth-canonical pairs** — 49% are the same search under a rotation — and on 9 of the 29 the entire tied set is one start listed several times. **Where the tie is real it is inconsequential:** 12 of the 29 tied sets have every member solving and 16 have none solving, so no tie-break can change the verdict; exactly **one** presentation (533, 25 nodes against 1,000) is a genuine decision, and it is the same row that has driven every margin in this study.

**The canonical dedup buys exactly zero.** It is not a no-op — 42 of 60 top-3 lists spend at least one slot on a search an earlier rank already ran (56 slots), and dropping the duplicates changes the top-3 *set* on all 42 — yet solves, median, mean and the whole 61,627-node bill are identical, win/tie/loss **0/41/0** at budget 1,000 and **0/52/0** at 10,000. This does not refute [`cov_top3/RESULTS.md`](../results/stable_ac/cov/cov_top3/RESULTS.md); it measures the abel half of it, and that census had already found the waste to be a `len`-arm problem (`len` 325,963 nodes on 126 repeats against abel's **707 on 38**) while flagging its own re-score as a lower bound. This file removes that limitation — it re-ranks the whole enumerated family, so the dedup really does promote never-searched candidates — and the abel gain is not small but zero. Keep the dedup as hygiene so `k` means *k distinct searches*; never quote it as a gain.

**Where the headroom actually is.** Not in a wider `k` either: the rank of the first solving candidate reaches 40 of 45 at k=1, 41 at k=2, and **k=3→k=5 buys 0**. The four rows between 41/60 and the oracle's 45/60 sit at ranks 8, 8, 49 and 44 out of 109–138 candidates and solve in **410–632 nodes** — cheap solves ranked far down, i.e. a primary-ranking failure, not a budget or tie-break failure. All four share one cause: the solving candidate is **one abel step above the minimum shell** (abel 3 against the rank-1 pick's abel 2), and on two of them it is also much longer (41 and 37 against 24), so both keys point away from it. Over the 45 solvable rows the minimum shell is right 40 times and wrong 5. Three class-quota policies (3 from the minimum shell; 2 + 1 from the next; 1 from each of the three lowest) recover none of them, and the pure-shell policy loses one — `(abel, total)` already spills into the next shell whenever the minimum holds fewer than 3. The open question is **when to skip abel's minimum shell**, and it is not a tie-break question. Gates: `pytest tests/heuristic_search -q` → **97 passed, 2 skipped** (excluding the untracked `test_fftvar.py`).
