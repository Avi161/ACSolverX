# 10-08-2026

## 18:52:00 UTC · `PENDING`

First push of branch `experiments/ppo` — the PyTorch port of the Two-Hump PPO baseline, plus the A100 Colab driver that replicates it. Colab clones from `origin`, so nothing on this branch could run there until now.

**The port** (all new, `numpy`/`torch`, the JAX tree stays a read-only spec): [`experiments/ppo/acs_data.py`](../experiments/ppo/acs_data.py) (loading, re-padding, and the 634 pin *derived* from the data), [`acs_moves.py`](../experiments/ppo/acs_moves.py) (batched S-move + upstream's Booth), [`acs_spec.py`](../experiments/ppo/acs_spec.py) (scalar transliteration of `envs/ac_s.py`, the oracle), [`acs_env.py`](../experiments/ppo/acs_env.py) (`VecACS` with all three wrappers fused), [`policy.py`](../experiments/ppo/policy.py), [`transplant.py`](../experiments/ppo/transplant.py) (orbax/flax → torch, 95/95 arrays, no silent skips), [`ppo.py`](../experiments/ppo/ppo.py), [`beam.py`](../experiments/ppo/beam.py), [`run_ppo.py`](../experiments/ppo/run_ppo.py) (stages `convert`/`parity`/`train`/`beam_eval`).

**This push adds** the Colab driver [`experiments/notebooks/ppo/ppo_baseline.ipynb`](../experiments/notebooks/ppo/ppo_baseline.ipynb) (CONFIG / SETUP / RUN + a TABLE cell, which earns the fourth slot by aggregating across sessions and re-printing without touching a GPU stage) and [`experiments/ppo/results_table.py`](../experiments/ppo/results_table.py), which builds the paper-format table from beam jsonls over `1190MS` **and refuses anything else** — a training log's `num_solved` counts over 156,762 rows on the AC-19 arm, not 1190.

Two fixes behind it. The beam jsonl is a resume key, so its filename now carries the *policy*, not just the eval knobs (`beam_tag` folds in the checkpoint stem and its update count): two seeds, two arms, or one arm at two update counts previously shared one file, and the second run would skip every presentation and report the first one's solve count as its own. And `stage_train` no longer pins more envs than exist when `NUM_ENVS` is shrunk for a smoke run — it clamps and says the run is not a paper arm.

Recorded in [`experiments/ppo/PLAN.md`](../experiments/ppo/PLAN.md): the paper's "+ AC-19" row is `data/AC19_extended.txt` (its first 634 lines *are* the first 634 of `1190MS`, which is exactly `parallel_sample[:634] = False`), **not** `data/AC19.txt` (0-length shared prefix, 171/1190 rows, pins nothing); `NUM_ENVS = 2380` on the `1190MS` arm is a reconstruction, since only the AC-19 arm's config shipped; and the shipped `610model` is step 1000 of 4376, so beam-decoding it over `1190MS` is a replication of ~607 with no training at all — the notebook's third rung and the gate on everything after it.

`.venv/bin/python3 -m pytest tests/ppo -q` → **51 passed**.
