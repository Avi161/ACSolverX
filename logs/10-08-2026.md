# 10-08-2026

## 19:20:00 UTC · `32bc4a6a`

Shipped the abelian-mass **top-3 CoV** rule over all 640 ms640 presentations at budget 100,000, as a two-stage pipeline sharded across two parallel Colab sessions. Stage A enumerates each presentation's subword-CoV family, ranks it by `abel(r1,r2) = |σx(r1)|+|σy(r1)|+|σx(r2)|+|σy(r2)|` and freezes the top 3 — **1,920 picks, zero search nodes, ~2 s**; the selection has to be frozen on disk before any search runs, or two parallel sessions can disagree about what they are searching. Stage B searches those starts in rank order and **stops at the first solve**, so a presentation costs at most 3 × 100,000 = 300,000 nodes, and records `cum_nodes` per row. The key and its tie-break are imported from `abel_topk_cov_b1k`, never re-implemented, and the tests pin that Stage A's independently-enumerated ranking reproduces the validated budget-1,000 selection on all 60 subset-60 rows — the ms640 run measures the same rule the subset-60 result reports. *(The modules, doc, tests and results directory of this section were renamed in the 21:05 section below; the links there are the live ones.)*

The node-matched control costs zero new search: a greedy search at budget `B` is exactly the first `B` pops of a longer one, so plain greedy at 300,000 is the frozen `greedy_1000000_640_*.jsonl` read with `solved and nodes_explored <= 300000` → **638/640, unsolved {634, 635}** (one 100,000-node search alone is already 634/640). Which means ms640 is nearly saturated at this budget and a solve-count headline has almost no room in it — the [control-with-no-dynamic-range](../experiments/lessons/control-with-no-dynamic-range.md) shape. The frozen 10,000-node sweep settles four of the six movable rows for free: abel top-3 takes **634 at rank 1 in 7,840 nodes and 635 at rank 1 in 7,875** — exactly the two the control cannot reach, a ~38× cost win — while no CoV in the family solves 636–639. So the dimension with real dynamic range here is **cost**, and 636–639 are the only genuinely open headline rows.

Also landing with its runner: [`experiments/heuristic_search/runners/abel_topk_cov_b1k.py`](../experiments/heuristic_search/runners/abel_topk_cov_b1k.py) + [`tests/heuristic_search/test_abel_topk_cov_b1k.py`](../tests/heuristic_search/test_abel_topk_cov_b1k.py) and its subset-60 writeup [`results/comparison/ABEL_TOPK_COV_B1K.md`](../results/comparison/ABEL_TOPK_COV_B1K.md). Gates: `pytest tests/greedy tests/stable_ac tests/analysis -q --runslow` → **1123 passed, 239 skipped**.

## 21:05:00 UTC · `PENDING`

Re-cut the ms640 census as a **two-rule comparison**, one Colab session per rule, each over all 640 presentations, on the user's instruction: the deliverable is `nodes_explored` and `path_length` against plain greedy, not another solve count. `abel` (abelianized magnitude) and `len` (shortest transformed pair, `KEYS["len_only"]`) are both imported from `abel_topk_cov_b1k` — and they are not two names for one thing: the two rules pick the same top-3 **set** on only 5 of the 640 presentations. Sharding by rule replaces sharding by row (`CHUNKS` stays, defaulted to 1, for splitting one arm further). Renamed accordingly, nothing having run yet: `abel_top3_manifest.py`/`abel_top3_run.py` → [`cov_top3_manifest.py`](../experiments/stable_ac/cov/run/cov_top3_manifest.py)/[`cov_top3_run.py`](../experiments/stable_ac/cov/run/cov_top3_run.py), `ABEL_TOP3_MS640.md` → [`COV_TOP3_MS640.md`](../experiments/stable_ac/cov/run/COV_TOP3_MS640.md), `tests/stable_ac/test_abel_top3.py` → [`test_cov_top3.py`](../tests/stable_ac/test_cov_top3.py), `results/stable_ac/cov/abel_top3/` → `results/stable_ac/cov/cov_top3/`, and the two chunk notebooks → [`cov_top3_ms640_abel.ipynb`](../experiments/notebooks/stable_ac/cov_top3_ms640_abel.ipynb) + [`cov_top3_ms640_len.ipynb`](../experiments/notebooks/stable_ac/cov_top3_ms640_len.ipynb) (`RULE` is the only knob that differs). The abel manifest's selection is byte-identical to the one committed at 19:20 — only the new `rule` field was added.

**The rule↔manifest binding is enforced, not documented**, because with two manifests on disk a stale path plus the other rule's name is a wrong experiment wearing a plausible filename and nothing in the results could reveal it: the manifest path is *derived* from the rule, every manifest row carries its `rule`, the loader raises on a mismatch, and the rule is the first field of `_run_prefix` so the arms can never resume into each other. Four new tests cover it, including an end-to-end `run(rule=other)` refusal.

Rows now carry everything the comparison needs with no join — the search (`solved`, `nodes_explored`, `path_length`, the `path_moves` tuples `verify_results` replays), the pick, the presentation's running `cum_nodes`, **and** the untransformed route's own numbers for the same presentation (`base_solved`, `base_nodes_explored`, `base_path_length`, read once from the frozen 1M baseline). `summarize()` leads on the paired nodes/path comparison over the presentations *both* arms solved — median/mean/max plus win/tie/loss on each — after the solve counts, and prints that a CoV path certifies the transformed pair, never the original. `compare_rules()` scores the two arms on their intersection.

Smoked by exec'ing the real CONFIG/SETUP/RUN cells of both notebooks at budget 1,000 over 20 stride-sampled presentations (`CHUNKS=32`), then the head-to-head cell: abel 20/20 and len 19/20 against plain greedy's 17/20 at one search and 19/20 node-matched, with abel mean 66 nodes / mean path 16.9 against plain greedy's 547 / 24.9 on the same rows — the shape the full run is expected to have. `verify_results` had never been fed a row of this format, so a 5-presentation file per rule was generated at budget 1,000 and run through it before the user spends core-hours: **5/5 certificates verify on each arm, exit 0** — the new `mode` strings and the `base_*` fields do not confuse the verifier, and the "Afterwards" step in the method doc is now a checked instruction rather than an assumed one. Gates: `test_cov_top3.py` 50 passed; `pytest tests/greedy tests/stable_ac tests/analysis -q --runslow` → **1149 passed, 239 skipped in 228 s**.
