# 29-07-2026

One file per day; **one section per push**, headed with UTC time and tip commit short SHA.

## 03:29:00 UTC · `PENDING`

Repo-wide reorganization, no behaviour change intended and none found: all six Colab notebooks consolidated under [`experiments/notebooks/`](../experiments/notebooks/README.md) (byte-identical across the move), the two `.py` files that were living inside `results/` moved out to [`experiments/stable_ac/cov/figures/`](../experiments/stable_ac/cov/figures/), and five test files relocated from `tests/greedy/` to `tests/stable_ac/` + a new `tests/analysis/` — with the mandatory gate widened to `pytest tests/greedy tests/stable_ac tests/analysis -q` so no coverage is lost. Three new maps added ([`tests/README.md`](../tests/README.md), [`data/README.md`](../data/README.md), [`experiments/notebooks/README.md`](../experiments/notebooks/README.md)) plus [`results/stable_ac/README.md`](../results/stable_ac/README.md) and [`results/comparison/README.md`](../results/comparison/README.md), the last two areas under `results/` that had none.

An audit pass over the whole tree then fixed the rot it turned up: two files hardcoded an absolute `sys.path` into `.claude/worktrees/hsearch-hyper`, **a worktree that no longer exists**; seventeen repo-root resolvers counted `os.path.dirname()` levels (or, in `orbit_greedy.py`, anchored on `os.getcwd()`) instead of walking up to `experiments/`+`data/`; and ~25 backticked paths across docs, prompts and lessons pointed at files moved by earlier reorgs. Two real defects were found by the verification pass rather than by the audit: `make_figs.py` had no `__main__` guard, so moving it into a package made *importing* it overwrite two committed result PNGs, and `cov_summary.py`'s old `ACS` was one level short of the repo root — it resolved to `<repo>/results`, so its `COMBINED` never existed and `_nodes_1m()` silently returned `{}` on every run ever made. Both fixed; the second means that script no longer reproduces its own committed CSVs, which is stated in its docstring and left for a separate labelled commit.

Verification: full suite **1194 passed, 0 failed** (289 skipped) — the one long-standing red, `test_transformed_flat_repads_to_cap`, now `importorskip`s the `jax` it needs and that neither this venv nor CI installs, so the widened gate is green out of the box instead of red. `--runslow` gate 1099 passed; `verify_proofs.py` prints `ALL 137 EDGES VERIFY`; the frozen `benchmark/` **regenerates byte-identical**; `results/greedy_baseline/` and the upstream JAX stack are untouched.

**Added:** [`tests/README.md`](../tests/README.md), [`data/README.md`](../data/README.md), [`experiments/notebooks/README.md`](../experiments/notebooks/README.md), [`results/stable_ac/README.md`](../results/stable_ac/README.md), [`results/comparison/README.md`](../results/comparison/README.md), [`experiments/analysis/__init__.py`](../experiments/analysis/__init__.py).

**Moved:** 6 notebooks → [`experiments/notebooks/`](../experiments/notebooks/); [`cov_summary.py`](../experiments/stable_ac/cov/figures/cov_summary.py) + [`make_figs.py`](../experiments/stable_ac/cov/figures/make_figs.py) out of `results/`; `test_solvern`, `test_solvern_fast`, `test_verify_results`, `test_word_families` → [`tests/stable_ac/`](../tests/stable_ac/), `test_combined_benchmark` → [`tests/analysis/`](../tests/analysis/).

**Changed:** [`CLAUDE.md`](../CLAUDE.md), [`experiments/CLAUDE.md`](../experiments/CLAUDE.md), [`README.md`](../README.md), [`PRUNED.md`](../PRUNED.md), [`pytest.ini`](../pytest.ini), [`.gitignore`](../.gitignore), [`.github/workflows/tests.yml`](../.github/workflows/tests.yml), and ~50 docs/scripts under [`experiments/`](../experiments/), [`results/`](../results/), [`prompts/`](../prompts/) and [`tests/`](../tests/).
