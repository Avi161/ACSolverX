# 14-08-2026

## 23:23:58 UTC · `PENDING`

**covmeet**: the CoV-only collision search over the 124 unsolved classes, built to the user's sketch after measuring its two load-bearing quantities (branching ≈ 3 orbits/state after Aut-min collapse of ~54 raw CoVs; `aut_min` flat in input length, so NO relator-length cap anywhere — `reject_len` uncapped, family tag bumped to `subnc2pxysbnolim`). Seeds: all 124 at `best_rep` (the 39 reduced classes start at their CoV-descended rep). Two deliverables per run: merges (two seeds' cones meeting at one orbit ⇒ stably AC-equivalent ⇒ `124 − S`) and per-class descents below the seeded aut-min (`[DROP]` / `improved_below_seed`).

**Added:** [`experiments/stable_ac/cov/meet/covmeet.py`](../experiments/stable_ac/cov/meet/covmeet.py) (engine: exact-key store, per-seed bitmasks + union-find, bucketed shortest-first open set, append-only jsonl with torn-tail repair + fsync-per-wave + resume-by-replay, TIME-based heartbeat, MemAvailable guard), [`verify_covmeet.py`](../experiments/stable_ac/cov/meet/verify_covmeet.py) (structural replay + segment-by-segment edge replay — chains junction at canonical reps, never concatenated), [`covmeet_vast.ipynb`](../experiments/notebooks/stable_ac/covmeet_vast.ipynb) (CONFIG/SETUP/RUN/STATUS for a vast.ai CPU box; SMOKE_RUN=True first, download/re-upload OUT_DIR = the crash contract), [`meet/README.md`](../experiments/stable_ac/cov/meet/README.md), [`tests/stable_ac/test_covmeet.py`](../tests/stable_ac/test_covmeet.py).

**Verified:** 18 tests green (resume==uninterrupted, torn tail, merge via planted child, drop detection — a real 22→18 descent at depth 2, byte-determinism, serial==parallel, wave-is-not-a-beam: all-duplicate children never stop the run). Notebook exec-checked end to end against this branch: fresh smoke → Restart-&-Run-All resume (134→252 expanded across restarts) → verify sample exit 0. Pre-existing on this branch, not this push: `tests/stable_ac/test_mu_ladder_big.py` cannot COLLECT on py3.11 (`mu_ladder_big.py:653` uses a py3.12-only nested f-string, from commit `b736f001`); the rest of `tests/stable_ac`, `tests/greedy`, `tests/analysis` ran instead — counts in the commit message.
