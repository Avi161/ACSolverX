# 16-08-2026

## 16:22:04 UTC · `PENDING`

Audited the downloaded vast.ai covmeet run (snapshot 06:58:30 UTC — 14,627,414 orbits, 9,336,325 expanded, 0 merges) and fixed the one real defect it exposed. The store itself verifies: sha256 + exact body length, 300/300 sampled orbits are `aut_min` fixed points with abelianization det ±1, 25 chains replay segment by segment (3,840 segments), and the 124/124 class count re-derives from the masks alone — the null is real, not a dead detector. The defect was bookkeeping: [`covmeet.py`](../experiments/stable_ac/cov/meet/covmeet.py) wrote `summary.json` only in `run()`'s `finally:`, which SIGKILL skips, so a preempted 25 h session left a 3-minute smoke summary (28,224 expanded) beside the real snapshot — and since `verify_covmeet` hard-fails on that disagreement and `report_covmeet` gates on the verifier, a healthy store was unreportable. Now `_write_summary()` runs immediately after **every** `save_snapshot()` (snapshot first, summary second), plus a `regen_summary()` recovery path and a `__main__` to invoke it; `ENGINE_TAG` deliberately unchanged, since bumping it would make `load()` reject the existing snapshot. Three new tests in [`test_covmeet.py`](../tests/stable_ac/test_covmeet.py) (34 green, kill-path mutation-checked), the audit write-up in [`AUDIT_2026-08-16.md`](../results/stable_ac/covmeet/AUDIT_2026-08-16.md), and the lesson in [`summary-written-only-in-finally.md`](../experiments/lessons/summary-written-only-in-finally.md).
