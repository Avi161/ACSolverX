# Independent final-report review

Verdict: the numerical result and the ordinary-versus-stable certificate distinction pass this independent cross-check. The reviewed draft requires the limited wording corrections below before unqualified final acceptance. This is a document/provenance review, not a new full replay of every source certificate. No mathematical search or heap work was performed.

## Corrections requested

1. **Catalogue section2: a complete boundary pass is required.** The draft describes replacement of “a lowest-index block H” and then says the selected span strictly decreases. Replacing just one block may leave other occurrences of the same extreme. The proved source `astra_patterns.md` requires replacing every occurrence/run at the chosen extreme with the same shifted donor. State that each block replacement has the exact two-donor ledger, and the complete pass removes the extreme and strictly lowers span. This is the only substantive theorem-scope overstatement found.

2. **Disclose the recorded physical-budget exception.** `complement_audit.md` explicitly records that the aca_0 preflight was repeated during the endpoint follow-up:2,000 physical checks of1,000 distinct identifications, adding no coverage. Total physical checks are124,055 instead of the123,055 main-pass count. Add this short exception to the final budget discussion or the catalogue follow-up paragraph. It does not invalidate any certificate or the85-row result, but should not disappear from final reporting. The later124-row conjugated-attachment experiment is a separate experiment and is not implicated.

3. **Catalogue section4: introduce k.** The displayed coupled exchange uses `T^(-k)` without declaring k. Its source theorem permits any integer k, and the finite recognizer chooses among the displayed breakpoint candidates. Add “for any integer k” before the identity.

4. **Catalogue section11: qualify the trivial complement-basis return.** “Choosing a complement basis of F2 itself gives back the original problem” should say that the natural unit-lift or synchronously transported marking gives an ambient return. The independently reviewed statement does not classify arbitrary kernel markings chosen for the same complement subgroup. This qualification already appears in `two_complement_independent_theory.md` and the new scope note.

5. **Catalogue section6: expose the sign operation.** Starting with strict stabilization at z, invert its unit relator before appending the normal product for w, obtaining `z^-1 w`. The draft currently omits that inversion in the explanatory sentence. It is an available elementary move and does not undermine the theorem.

6. **Final export metadata is still prospective at the reviewed snapshot.** `FINAL_REPORT.md` describes the table's `final_snapshot` status, while the JSON currently says `interim_pending_deadline`. Finish the planned final refresh and manifest pinning before presenting the report as the final deadline export. No numerical disagreement was found.

All six items were communicated to the root; this review does not edit root-owned files. The final status change and word edits should be read back by the root before closure.

## Claims independently cross-checked

The124 distinct table rows sum to archival2446, saved starting2356, rank2 best2356, and all-rank best2191. Every reported best word tuple has exactly its stated total length and row count; the retained ranks are39 rank2,79 rank3,6 rank4. Exactly85 rows improve over the saved start and all124 remain unsolved. The165-letter saving and the contribution chain2199,2198,2197,2191 are arithmetically consistent. Historical36 reductions are correctly separated from new work. No claim of124 proved distinct AC classes is made.

The stable certificate wording correctly retains known input triviality, exact word/basis witnesses, unexpanded finite normal products, possible higher-rank helpers, and the absence of elementary stable expansion costs. All-relator counting is explicit. The ordinary rank3 suffix and the rank4 defining-word gains are supported by their prior independent audits. The examples aca59 length17 and aca24 length15 count every current relator.

The geometric92-tuple exclusion is correctly restricted to exact presentation complexes:75 Kuratowski witnesses and17 rigid phase-count failures. It is not presented as an AC obstruction. The current capped conjugated-complement experiment is correctly unknown on all124 inputs; the finite theorem is not confused with its incomplete evaluation. The exact root cyclic-complement negatives are kept separate from capped endpoint follow-ups.

The final marked-basis experiment totals agree with `two_complement_unit_gate_independent_audit.md`:15 projections,4224 prefix units,10776 pops,4227 emitted ordinary moves; the Euclidean attempt uses28 plus972 units and430 ordinary moves. The additional16 endpoint image evaluations are explicitly outside its1000-unit attempt. Separate q3 attempts are not represented as part of a cumulative per-presentation1000-unit portfolio. The displayed component clocks retain their CPU/wall/cooling scope; no whole-session runtime or equal-cost claim is inferred from heterogeneous units. The5374-certificate/3042053-step intermediate audit agrees with its source and is not a claim to have expanded the stable macros.

The newly inserted q3 paragraph and Euclidean section12 were read during this review. They correctly distinguish literal q3 ambient returns from the d7,q2 continuation endpoint. That endpoint has Aut-minimal cyclic total21, so it differs from the original total20 orbit; the text correctly avoids inferring that the raw projected pair has the same classification. No new solve, rank2 gain, universal terminal theorem or literature-priority claim is asserted.

## Reviewed snapshot

The documents remained under active root editing. These hashes identify the latest versions read for this review, rather than asserting that later final files are identical.

* `FINAL_REPORT.md`: `3c3542b0fc6d12b1c3bf6cd4d6f8e8a367dc2a3e01a96a3d4aa9ebaed6509632`.
* `THEORY_CATALOG.md`: `a12275322010a8a661654bab7a43ddf97193ef829e8efe69d631a030687156cd`.
* `u124_final_table.json`: `0f27974a4e630988a96bf506aa08b14d8049d69e74137cc3aa4134a5dae732ad`.

Table status: `interim_pending_deadline`; timestamp `2026-09-12T06:02:59.063190+00:00`.
