# Endpoint Nielsen audit

Pass:124 rows, 2280 stored states, 1392 strictly decreasing map steps and 29376 image evaluations; maximum 544 per input. All eight maps and listed inverses compose to the identity in both orders. Every stored image and canonical pair was checked with independent word routines.

No row improves its saved starting length or solves. These explicit ambient-map traces do not supply ordinary AC certificates. No endpoints or gates were rerun.

The gate ledger confirms 2280 endpoint rows, 2280 globally distinct pairs, 933 summed BS charges and zero terminal candidates. The362 BS matches/stalls are recorded summary counts; individual negative calls are absent from the ledger, so those two counts were not independently recomputed.

Runtime: 0.133006s CPU / 0.134335s wall. Source hashes and scope limits are in `endpoint_nielsen_audit.json`.
