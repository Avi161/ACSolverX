# Paired donor commutators: PASS

The replacement multiplies target R on the right by
`D^-s c^-1 D^s c`, with s=±1. Its two factors use the same retained donor,
opposite signs, and conjugators empty/c respectively. Direct free reduction
and the independent generic normal-product checker verify the exact result.
The correction has zero exponent sum in every generator, so it preserves the
target's abelianization vector before normalization.

Twelve signed operations on the known-trivial pair (ab,b) pass independently.
A40-unit planted probe yields ten checked endpoints, including Whitehead tails;
every event boundary replays and the shared budget is respected. The current
dispatcher also verifies the commutator-specific metadata. Invalid boolean
sign metadata is rejected.

The paired update is an ordinary AC composite. A following ambient Whitehead
map retains its separate stable-realization scope. Preserving exponent sums
does not itself prove that the new donor is BS-shaped, shorter, or useful;
those are separately checked by downstream recognizers. No module bug or
new census gain is asserted here.

Reproduce with `verification_commutator_checks.py`; results and source hashes
are in `verification_commutators.json`. No census search was run.
