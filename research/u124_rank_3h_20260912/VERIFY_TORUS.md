# Full retained-torus collector — PASS

The central consequence and the power consequence are both required. For D=z⁻¹(hᵈzᵃ)ᵏ, a<0, let c=hᵈ and n=1−ak. The retained donor gives [c,z]=1 and cᵏ=zⁿ; these imply D again. The compiler retains D and expresses every correction as a product of its conjugates. It never replaces D by the power consequence alone. The latter replacement would lose information for k>1, as the author's finite-free-product counterexample shows.

The group has the amalgam description ⟨h,w | hᵈ=wⁿ⟩ with z=wᵏ. Since gcd(k,n)=1, the z residues give the required transversal modulo the infinite central subgroup generated by c. The quotient is C_d*C_n. Thus the integer central exponent and reduced alternating positive residues uniquely determine an element. The cited normal-form background is [Elder, Elston and Ostheimer, Lemma34 and Corollary35](https://arxiv.org/pdf/1201.4363), printed page14; the retained-alphabet metric argument is derived in the current research note.

The allocation lower bound covers arbitrary spellings, including extra quotient-cancelling syllables. Remove a quotient-trivial syllable into a central accumulator while keeping its original signed power and length, then merge adjacent same-axis syllables by the triangle inequality. This reaches the unique reduced quotient sequence without increasing length. Combine the removed central powers separately by axis. Its surviving exponents and the two extra zero-residue slots therefore give a feasible allocation of cost at most the original spelling. Conversely any allocation with total A+kB=Q represents the same element. This proves equality with the stated scalar minimum.

For each axis, the allocation cost is convex: negative unit allocations first cross the largest residues, then cost the modulus, while positive units always cost the modulus. All breakpoints occur at negative integers up to the number of residue slots. After substituting A=Q−kB, checking floors and ceilings of those breakpoints finds an integer minimum. The objective grows in both tails, so no arbitrary central-exponent cutoff is needed. The reconstruction has no hidden cancellation: nonzero residue blocks remain nonzero and alternate, while a nonzero extra axis slot appears only when that axis has no surviving residue slot.

The incoming block ledger gives old·P=normal and the outgoing ledger gives packed·Q=normal. Hence old·P·Q⁻¹=packed. Conjugation by the remaining suffix and then the inverse circular frame places this right correction in the original target. Only the retained nontarget donor occurs in the factors. Every resulting event can be checked as ordinary AC with no known-triviality premise.

`verification_torus_checks.py` uses an independent letter-stream normal signature and direct integer allocation oracle. All85 signed words through length3 match exact minimum lengths across47 normal forms; nine scalar minima and22 signed sparse rank-four chains pass. Every central/power consequence and incoming ledger is freely checked, and every final event is independently replayed with `known_trivial=False`. All33 saved composed pilot records also pass with no new gain beyond imported seeds. Results are in `verification_torus.json`; no census run was repeated.

Completeness concerns the fixed linear word metric for each processed maximal two-generator block in this one-donor quotient. It does not assert a cyclic conjugacy minimum, a shortest full presentation, exhaustive donor recognition under all bases, or stable-AC minimality. The budget may leave other recognized models and blocks unprocessed, while every returned certificate remains exact.
