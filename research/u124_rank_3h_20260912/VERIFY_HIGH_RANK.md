# Retained high-rank short-relator delivery — PASS

All124 retained tuples have every relator of length at most3, ranks7–12, and total length3444. Every original row and all811 new defining rows are retained. Every selected pair, exact expansion, cyclic frame, tuple count, decreasing excess and charged unit was independently checked. The new suffix has no generator removal.

The separate full certificate file is [all124_retained_high_rank.jsonl](verification/export/all124_retained_high_rank.jsonl), with [manifest](verification/export/retained_high_rank_manifest.json). It embeds1164 composite events from the saved rank-two inputs of total2356, including explicit normalization where CURRENT used a different relator frame. There are no deferred lineage segments. Elementary stabilized expansions are not emitted.

This individual-relator objective is separate from minimum-total CURRENT2162. Neither the triangular form nor higher rank proves a solve or easier search. The retained output required12234 named algebraic work units; the later fixed-rank flip pass added117984 units and changed no endpoint. All124 flip endpoints therefore use the same exported certificates.

The construction terminates because every two-letter definition lowers the nonnegative sum of excess lengths above3; its own new row has length3. A balanced tuple with every row at most2 is much stronger: triviality forces each signed relation-graph component to be a tree with one singleton. Ordinary AC propagation then produces all singleton generators at the same rank. An ungrounded component has a nontrivial C2 quotient; in particular all exactly-two-letter rows admit the all-generators-to-C2 map. These facts do not imply that a triangular tuple is near a terminal one.
