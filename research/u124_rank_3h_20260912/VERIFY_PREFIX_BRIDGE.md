# First-commutator and conjugate-base bridge audit — PASS

The first-helper collector has an exact free-word proof. Set
`c=q b q^-1 b^-1`. For positive `n`,
`q^(n-1)c(q^-1 c)^(n-1)b` freely reduces to `q^n b q^-n`.
For negative `n`, the corresponding expression is
`q^n c^-1(q c^-1)^(-n-1)q b`. Inverse old letters invert the whole expression.
These formulas follow by repeated cancellation; they introduce no truncated
higher commutators. The template length is at most `3|n|+1`.

`verification_collection_prefix.json` records 56 independent signed identities,
including heights ±100 and generator labels above `10^24`. Three saved full
control chains replay, as do four signed sparse compressions and five tiny
budget controls. Three corrupted axis/height/exponent metadata examples fail.
The saved aca43 prefix still reaches length 16 from its length-18 intermediate
in one actual helper addition; it reconstructs an already found gain.

The independent dispatcher now verifies the signed axes, exact commutator
definition, shifted chunk heights, excluded-conjugate fallback, full assembled
row and charged frame count. Core template expansion, every original relator,
the defining row and all normalization data remain mandatory. Mode and frame
selection are sufficient candidate heuristics, not a shortest-word theorem.

The conjugate-base bridge also passes. If `h=x^-1 y^e x` and `e` divides `m`,
then `x^-1 y^m x=h^(m/e)` exactly after expanding the retained helper. The
opposite orientation uses divisors of `n`. The compiler does this through
ordinary normal-product repacking with the new defining donor, then tracks
both retained donor indices through canonical sorting. All other relators
are processed and retained. Its token dynamic program uses exact, sufficiently
recognized blocks; it does not claim arbitrary quotient-group geodesicity.

Four signed sparse chains, twelve partial-budget chains and one rank-eleven
to rank-twelve chain pass independent replay. A partial bridge may consist
only of the valid defining addition; it is marked incomplete. Neutral or
longer bridge endpoints are deliberately retained for the subsequent
Whitehead/peeling attempt. The saved 33-row pilot replays and adds no strict
gain beyond its imported seed. These checks are in
`verification_conjugate_bridge.json`.

The rejected unrestricted replacement is materially different: keeping
`h=x^-1 y^e x` but replacing the original BS donor by `h^m y^(-en)` multiplies
its abelianized row contribution by `e`. With a stable-exponent-one companion,
the determinant magnitude changes from `|m-n|` to `|e(m-n)|`. For consecutive
BS parameters and `e>1` this is nonunimodular, even when `e` is coprime to `m`.
Six exact determinant controls verify that obstruction. The safe divisibility
compiler never performs this replacement.

The stable additions retain the known-trivial input premise. All subsequent
normal-product operations and full inverse maps are checked by exact free-word
replay. Neither audit reruns a census search.
