# Independent commutator collection and dictionary exchange audit — PASS

`verification_collection_checks.py/json` independently replays the frozen
collector's two saved solved controls and both saved census pilots. The
hidden commutator control reaches the empty tuple through rank three; the
larger control retains eight defining relators, reaches rank twelve, removes
the old axis to reach rank eleven, and then reaches the empty tuple. These
are planted controls with explicit triviality proofs, not new census solves.

For a fixed sign, write `c_0=g` and
`c_(j+1)=x^sign c_j x^-sign c_j^-1`. Then
`x^sign c_j x^-sign=c_(j+1)c_j` exactly in the free group. Applying this
substitution to every token, preserving order, gives the collector's
`2^|height|`-token expression for each conjugate. Inverting the complete
expression handles negative old letters; a separate signed ladder handles
negative heights. No commutator is discarded and no nilpotence hypothesis
is used. Fourteen independent signed identities include gapped labels above
`10^23`. Large heights fail the work preflight before exponential expansion.

The collector's complete row identity is also exact. Sweeping an oriented
row records each nonaxis letter at its current axis height. Moving the net
axis power to the left subtracts that total from every recorded height;
leaving it on the right leaves the heights unchanged. The normal-form
orientation, every chunk expansion, the full row expansion, and all retained
defining rows are checked independently. Cross-event checks track original
row labels through canonical sorting and require every declared definition
to appear in its planned stage. Left and right frames give distinct valid
endpoints in the control. Four corrupted metadata examples are rejected.

The total construction charge agrees with the declared orientation scans,
letter transitions, collection swaps, full-row expansions and all sequential
definition/template checks. Axis forecasts are an additional first-stage
charge in the probe. The saved controls report 85 and 612 total probe units;
their dictionary construction plus forecasts accounts for 70 and 270 units.
The remaining charges belong to forced removal and the recorded Whitehead/
peeling continuation. This audit replays saved witnesses; its new exact
controls are separate physical validation work.

`dictionary_exchange.py` correctly retains the initial Lemma-11 removal,
including longer removals, before testing a new definition and its Whitehead/
rank-peeling continuation. All emitted branches replay independently at
budgets 0, 1, 10 and 100; the used charges never exceed those allowances.
Choosing at most three removal endpoints and eight definitions per selected
endpoint is a bounded heuristic restriction. This is not an exhaustive
dictionary exchange theorem or a claim that every uphill removal is explored.

The collector and dictionary exchange pilots add no strict improvement
beyond their imported seed paths. Historical discovery work is kept separate
from the new probes' budgets in the JSON audit. All stable steps retain the
known-trivial lineage requirement; elementary word identities and ordinary
normal-product corrections are checked separately from that premise.
