# Retained-root preparations: PASS

The primitive-root preparer preserves the helper and every relator. If the
isolated helper definition u has canonical representative c^-1 u^s c, the
map h→c h^s c^-1 and its displayed inverse compose to the identity because
c avoids h. It then applies Whitehead maps chosen from the root word to the
whole tuple. The root does not use h, and each emitted map fixes h. Relator
normalization witnesses transport the defining donor index explicitly.

The independent controls replay six known-trivial plants on sparse and very
large generator labels, including conjugated cubes and commutator squares.
Ten emitted events pass both the ordinary event checker and independent root
metadata checks. Four endpoints expose a pure generator power. The
commutator-square cases are not falsely promoted to primitive roots.

This preparer is a sufficient witness generator. Partial descent and a local
minimum do not prove primitivity. Pure definitions are skipped. Root descent
can increase the total presentation length; its strict root-length objective
must not be mistaken for strict whole-tuple shortening.

The root-metric change maps h→a^(k-l)h with inverse h→a^(l-k)h. It changes
the retained definition h=a^k to h=a^l without removing a relator. Compilation
first performs the certified flow in the old basis, then applies this shear,
then cyclically repacks the donor and companion. This order preserves the
flow's original indexing. The two repacking ledgers use only the retained root
donor and independently check exact normal-product identities.

For rank3, the predicted endpoint cost is
`(|l|+1)+2+c_l(m)+c_l(n)+t+sum(c_l(e_i'))`.
Both signs of l have the same packing cost. A strict gain therefore has the
stated finite root-cost ceiling; negative ceilings can be discarded. Since
nonzero m,n each cost at least one token, the implemented denomination bound
`|l| <= L-t-4` is safely loose. No possible strict gain in this model is lost
by that bound. The no-pinch residue argument from the exact-flow audit keeps
stable length t fixed, so the predicted cost is a complete-tuple length.

Twelve signed rebases independently replay, including old and new negative
denominations and flows -1,0,1. One known-trivial BS(4,5) plant at1000 units
produces19 strictly shorter candidates, all checked. Triviality follows from
the companion x a^11=1, which makes the donor a^4=a^5 and kills every generator.

Scope remains rank3, no pre-existing cyclic Britton pinch, and a changed
absolute denomination. A completed flow search proves optimality only for
that tested denomination; the shared probe budget may leave others untested.
Donor-index remapping relies on the public normalized-input invariant. Every
ambient-map stable realization still requires known-trivial input lineage.
No algebraic, inverse-map or finite-denomination-bound bug was found.

Reproduce with `verification_preparer_checks.py`; results and hashes are in
`verification_primitive_root.json` and `verification_root_metric.json`.
No census search was run by the audit.

The saved nine-row root-metric pilot also passes independent exact-source
replay. Its only new gain is aca101, rank3 throughout: length21 → helper
denomination shear7→4 at length24 → ordinary cyclic repacking at length20.
All eight other rows have no gain. The pilot uses4375 new units with no imported
seeds. `verification_root_metric_pilot.json` retains the full boundary metrics,
and `verification_root_metric_pilot_checks.py` reproduces this read-only audit.
