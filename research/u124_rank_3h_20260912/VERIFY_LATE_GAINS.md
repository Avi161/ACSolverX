# Alias, neutral and uphill continuation audit — PASS

The single-commutator alias method retains the exact defining row and every old row. Its per-row choice is drawn from the complete recorded finite catalog: both circular frames and both direction policies are independently reconstructed, signed heights and linear chunk identities are expanded, and duplicate cyclic classes and catalog ordering are checked. This is completeness for that finite alias catalog, not for arbitrary defining words or automorphism orbits.

The coupled score is independently recomputed from the signed Whitehead graph. A separate dense residual-flow implementation proves each recorded cut optimal, and independently constructed signed generator images reproduce each resulting tuple length. All signed multipliers must occur. A selected accepted Whitehead map remains its own event, including its inverse. The saved tiny Cartesian catalog, all 96 signed-side controls, sparse rank-four example and four corruptions pass in `verification_collection_aliases.json`.

The pilot17 and remaining107 alias records partition the exact 124 IDs. Only aca_5 adds a new length gain: its saved rank-two start has length19, the helper tuple has length22, then two whole-Whitehead moves give19 and17. The previous incumbent was18. All six signed cuts and both selected row templates replay exactly.

The forced conjugate-exchange wrapper composes an already audited safe bridge, a forced old-axis removal, bounded repeated-word definitions and simplification. It excludes the exact isolation word and its inverse, which would simply restore the old basis. Signed sparse controls include uphill removals and recompressions; all saved 33-row pilot records add no new gain. Results are in `verification_conjugate_exchange.json`.

The neutral-automorphism exchange uses existing complete event types. Its two saved files partition124, with only aca_99 adding a gain beyond seeds: the retained path has lengths18→20→19→19→19→19→17 and ranks3→2→2→3→3→4→3. The neutral basis step and both helper definitions are included, and the final removal is checked against its actual input. See `verification_neutral_gain.json`.

The later full124 uphill screen has exactly three additional improvements beyond its seeds. aca_72 follows17→18→16; aca_80 follows17→18→17→16. aca_111 retains its earlier rank-four-to-three prefix and finishes with21→22→20 at rank three. Each new step first uses an ordinary retained-donor product, followed by whole-Whitehead descent. All124 records, source words, inverse maps, tuple counts and budget sums pass in `verification_uphill.json`, giving total2162. The screen's124000 new units exclude historical seed discovery costs. The bounded candidate selection does not prove the absence of other improvements.

The prefix collector's earlier new winners also replay: aca_44 has actual saved rank-two length18→helper rank-three17→Whitehead16, compared with incumbent17. aca_56 starts at incumbent rank-three18→helper rank-four20→Whitehead18→removal rank-three17. Exact pilot15/remaining109 partition and records are in `verification_prefix_gains.json`. None of these independent checks reran a census search.
