# Wave plan audit (C45) — applied before census

Source: ac-advisor `bc-3e4a1460-d65d-5ed4-a458-9aec7ff8892e`, 2026-09-12.
Coordinator recorded APPROVE, then the single-row k=3 census.

C45 is leftover defining-word `y` on aca_117. C35 is the x-census on
the six `YXXXyxx` rows. C44 is BEST-pair length descent.

## BLOCKERS

None.

## WARNINGS preserved

1. Single listed row `aca_117` only. Combo `(0,-1)` is for this
   `C_ab=(0,-1)` companion, not every companion of `D`.
2. `y ≡ C^{-1}` is abelianization, not free equality.
3. `k=1` is blocked by cyclic length 7 (`|C|=7`), not `|C|≥9` and not
   C35’s `|D|=7` x-obstruction copied onto y.
4. Orientation is C31: `R^+=C^{-1}`, `S=D`. C35 and C40 orientations
   are rejected. Witness replay bases are
   `{R+:C^{-1}, R-:C, S+:D, S-:D^{-1}}`.
5. Completeness is all 83,349 typed tuples in this bounded pool, not
   distinct reduced words and not `|F|^3`. Counts `(21,21,21,21)`.
6. Equal typed size 83,349 does not identify C35’s aca_117 x-census.
7. Disjointness is relative to prior typed donor-family censuses. C28
   and C44 touched this row under different predicates. C35 used C35
   orientation and x-targets. Not lumped with leftover `y` on other
   C35 rows (`L1∈{2,3,4}`).
8. aca_117 development-panel / n=2-wall is a disclosure, not a C16
   re-proof. Leftover `x` was C35. Do not re-run C35.
   `independent_checker=false`.
9. Observed minima are freely reduced product-word lengths, not a pair
   total, not a drop from BEST total 14, and not a C10 finish.
10. JSON `hit_replay_outside_scanner_capability` is capability, not a
    census hit. New files only; do not edit `test_campaign.py`.

## ALLOWED CLAIMS

- Unique listed-row y-combo `(0,-1)`, `L1=1`, `y ≡ C^{-1}` in abelianization.
- Bounded typed-pool negative, or an explicit ncl witness if persisted.
- Score remains `0/124`.

## VERDICT: APPROVE
