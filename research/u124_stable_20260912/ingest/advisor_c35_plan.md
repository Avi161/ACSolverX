# Wave plan audit (C35) — applied before census

Source: ac-advisor `bc-810a0a1e-4aa9-5a7a-96f7-6be7e4133e83`, 2026-09-12.
Coordinator applied the REVISE below, then ran the k=3 census.

## BLOCKERS

None.

## WARNINGS / REVISE items applied

1. Unimodularity is of the pair exponent matrix of `(D,C)`, not of `C`
   alone. `D_ab=(-1,0)` and `C_ab=(p,q)` give `det(D,C)=-q`; unimodular
   pair ⇒ `q=±1` and unique x-combo `(-1,0)`.
2. Hit recording persists ordered signed types, three factor words,
   conjugators, and a free-reduce replay `f1 f2 f3 = target`.
3. C35 is the last unused length-7 donor **in the listed shared-donor
   inventory**. The spelling already appears in C22.6 as a free identity;
   that is not an AC2 and is not this census.
4. Per-row `n_products == typed_nine_size(...)`. `independent_checker=false`.
5. Observed minimum length is a C35 census statistic, not a theorem and
   not a comparison with C29/C33/C34.

## ALLOWED CLAIMS

- Unique combo `(-1,0)`; orientation `R^+ = D^{-1}`, `S = C`.
- Bounded typed-pool negative, or a short ncl witness if explicitly recorded.
- Disjoint exact row pool from C29/C33. Score remains `0/124`.

## VERDICT: REVISE (applied before census)
