"""Ranking the Aut(F2)-images of a presentation by expected S20_MK2 cost (agent B2).

Public entry point: ``rank_images.rank(pair, radius=2, cap=48)``.  Everything else is the
training / evaluation pipeline over ``../atlas.jsonl``; see ``PREDICTOR.md``.
"""
