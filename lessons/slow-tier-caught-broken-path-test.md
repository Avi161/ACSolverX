# [2026-07-09] The slow tier caught a test that asserted a known-broken path [WORKS]
The default tier was green (446 passed) while **skipping 161 tests** — the entire `multiprocessing.Pool` path had never executed. Running `--runslow` immediately failed `test_resume_works_across_a_pool_run` with `IndexError: list index out of range`: re-running a *completed* pool sweep has `n_todo == 0` and dies on `todo[0]`. That is Gap 1 above, reached from a second, unplanned direction — so the bug is doubly confirmed, and the defect was in my *test*, which asserted a code path known to be broken. Rewritten to resume a **partially**-done run (seed 2 of 4 rows, then let the pool finish the rest), which is what "pool resume works" should actually mean. **Rules:** a green default tier proves nothing about what it skipped — check the skip count, and never push behind one. When a new test fails on a path you have *already documented as buggy*, the test is wrong, not the bug: don't widen the xfail.
