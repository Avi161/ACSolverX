# Lessons — 2026-08-27

### 2026-08-27 Keep forest and quotient metrics typed

- [TRAP] A lower bound for a reduced word in the stored forest alphabet `A/B/G` does not give the same lower bound for its canonical quotient label in the `c/t/T` alphabet. Never use a numerical length comparison across those two representations.
- [WORKS] Use forest-word distances only for forest incidence and boundary exclusion. For transported-label equality, work with the canonical quotient schemas or an exact label identity.

### 2026-08-27 Keep log mathematics shell-safe

- [TRAP] Passing backslash mathematics through shell-interpreted text introduced control bytes into `logs/27-08-2026.md` (`\\b` and `\\r` became backspace and carriage return).
- [WORKS] Write log text with `apply_patch`, prefer plain ASCII notation inside prose, and read back the changed line before committing the log binding.

### 2026-08-27 Import scratch proof modules explicitly

- [TRAP] `from .scratch import ...` fails in a guarded `python -c` process because it has no package parent.
- [WORKS] For a deliberately bounded guarded lookup, prepend the project-relative `.scratch` directory to `sys.path` and import the pinned module by its exact filename; keep the corrected run on the same narrow code path.

### 2026-08-27 Stage tracked ignored proof notes with the index

- [TRAP] Explicit `git add literature/...` can reject a modified tracked proof note because the parent proof directory is ignored by repository rules.
- [WORKS] For an already tracked proof checkpoint, use `git add -u -- <explicit tracked paths>` so staging updates the index without force-adding ignored untracked material.

### 2026-08-27 Keep ripgrep alternations literal for TeX searches

- [TRAP] A mixed regex alternation containing an unescaped TeX `\Phi` branch was parsed by `rg` as an invalid Unicode-property escape.
- [WORKS] Split TeX symbol searches into fixed-string `rg -F` queries or search the plain identifier first; do not mix raw TeX backslashes into an extended-regex alternation.

### 2026-08-27 Preserve TeX backslashes in JavaScript patch strings

- [TRAP] A JavaScript patch string consumed TeX backslashes in a Markdown math delimiter, so `apply_patch` could not match the literal context.
- [WORKS] Use `String.raw` for `apply_patch` payloads containing TeX; then read back the changed lines before continuing.

### 2026-08-27 Keep raw template delimiters out of patch payloads

- [TRAP] Markdown inline-code backticks inside a JavaScript `String.raw` template terminated the template and caused a syntax error before the patch ran.
- [WORKS] Split TeX-heavy and inline-code-heavy patches, or escape every template delimiter; prefer ordinary quoted strings for plain-ASCII log entries.

### 2026-08-27 Resolve scratch artifact names before targeted searches

- [TRAP] A targeted `rg` assumed a restricted-normalizer filename that was not present, adding a noisy missing-path error to an otherwise valid search.
- [WORKS] Use `rg --files .scratch` to resolve the exact artifact name before including it in a multi-file search.

### 2026-08-27 Refine equal-base pumped predicates fail closed

- [TRAP] [SUPERSEDED] The proposed `n>=2` pure-increment cell had 14 label pairs whose templates agree at the base but have different pumped canonical blocks; initially this looked like a threshold boundary.
- [WORKS] [SUPERSEDED] Splitting `n=2` from `n>=3` preserved the same 14 failures, proving that threshold refinement alone was not the repair.

### 2026-08-27 Normalize commensurable powers to one primitive phase

- [TRAP] Path and slot-zero schemas used different cyclic rotations of the same primitive core, so equal all-power words had unequal block keys at every pump base.
- [WORKS] Pass one common primitive reference across all commensurable schema families; let the fixed conjugators absorb phase rotations before comparing canonical pumped blocks.

### 2026-08-27 Shield raw pumps by the first affine increment

- [TRAP] A raw-locality record added the total base power length to an already absolute pump split, double-counting the base copies and overstating the protected action horizon.
- [WORKS] Certify `split + increment * core_length > horizon`, verify the exact base-to-next raw signature, and require positive central-length slope; this proves that every later insertion occurs beyond the finite raw boundary.

### 2026-08-27 Escalate Git metadata writes in restricted workspaces

- [TRAP] A normal `git add` failed with `Unable to create .git/index.lock: Operation not permitted` because the workspace permitted proof-file writes but exposed `.git` read-only.
- [WORKS] Keep proof edits sandboxed, then use the approved escalated Git command only for the explicit staging, commit, pull, and push checkpoint requested by the user.

### 2026-08-27 Split same-path delete and add patches

- [TRAP] One `apply_patch` payload with both `Delete File` and `Add File` for the same path is rejected as `multiple operations target` that file.
- [WORKS] For a deliberate full rewrite, delete and add in two separate `apply_patch` calls, then read back the replacement before staging.

### 2026-08-27 Refresh context after shared-agent edits

- [TRAP] A ledger patch used context captured before a documentation agent's final write and failed exact-context verification.
- [WORKS] After stopping or receiving a shared-filesystem agent, read the edited region again before applying the coordinating patch.

### 2026-08-27 Anchor insertions at stable section boundaries

- [TRAP] A prose insertion patch depended on an exact wrapped sentence and failed when its line-break context differed.
- [WORKS] For Markdown insertions, anchor the patch on the next stable section heading or a short unique equation boundary, then verify the inserted section by readback.

### 2026-08-27 Derive fixture endpoints from source rows

- [TRAP] A hand-written short-tail fixture advanced the final `C6` source positions to 21--22 even though the authoritative selected rows are 19--20.
- [WORKS] Bind endpoint fixtures to the first and last IDs returned by the exact source-row selector, while keeping independent family counts and semantic IDs as separate checks.

### 2026-08-27 Share primitive phase across quadratic families

- [TRAP] The raw certificate could normalize each powered schema independently because it never compared slot-zero and path labels; the quadratic boundary chords do compare them, and two unbounded terminal-P1 label equalities failed on cyclically rotated primitive cores.
- [WORKS] Before any cross-family all-power comparison, rewrite every commensurable path and slot-zero schema against one primitive reference, then verify unchanged direct expansions and bind the common reference in the certificate.

### 2026-08-27 Avoid unmatched zsh globs in routed proof searches

- [TRAP] A targeted `rg` command passed an unmatched `AK3_*UNARY*` glob to zsh, so the shell aborted before searching any valid paths.
- [WORKS] Resolve candidate files first with `rg --files literature/proofs`, then pass only existing literal paths to `rg`.

### 2026-08-27 Do not poll an agent's delete-add rewrite by fixed path

- [TRAP] A status poll ran `wc` and `stat` while an implementation agent had deleted rejected replay files before adding replacements, producing avoidable missing-file errors.
- [WORKS] During a shared-agent rewrite, use `rg --files .scratch` to test path existence first, or wait for the agent's completion message before inspecting the replacement.

### 2026-08-27 Require a clean-baseline assertion in hostile suites

- [TRAP] Twelve mutation tests passed even though the independent replay itself failed closed with `generation_failures` and `primary_semantic_projection`; the suite never asserted that the unmutated baseline verifies.
- [WORKS] Every proof-artifact hostile suite must begin with `verification_failures(clean_baseline) == []` before testing mutations, so producer/replay failures cannot hide behind successful rejection tests.

### 2026-08-27 Resolve replacement test class names before patching

- [TRAP] A clean-baseline test patch targeted the rejected draft's assumed class name, but the replacement file used `IndependentReplayTests`, so exact-context verification failed.
- [WORKS] After an agent replaces a file, read its current class/function header before applying even a small follow-up patch.

### 2026-08-27 Do not hide empty searches with shell control operators

- [TRAP] A final readback combined `git diff --check` and an expected-empty `rg` with `|| true`, violating the repository's no-command-chaining discipline.
- [WORKS] Run formatting checks and expected-empty searches as separate tool calls; interpret `rg` exit 1 as an empty result without shell control operators.

### 2026-08-27 Account for overlap in adjacent sed readbacks

- [TRAP] Two diagnostic `sed` ranges shared their boundary line, making a sentence look duplicated and causing an unnecessary removal patch to fail.
- [WORKS] When concatenating adjacent line ranges, make them non-overlapping or confirm suspected duplication with a targeted `rg -n` before editing.

### 2026-08-27 Patch long checkpoint paragraphs by short unique fragments

- [TRAP] A full-line replacement against a very long log paragraph failed exact-context matching even though the visible sentence was present.
- [WORKS] For one-clause corrections inside long log paragraphs, use the smallest unique surrounding fragment and verify the whole paragraph afterward.

### 2026-08-27 Never use raw JavaScript templates for log lines with inline code

- [TRAP] A second log repair used `String.raw` around Markdown inline-code backticks and failed in JavaScript parsing before `apply_patch` ran.
- [WORKS] Use an ordinary double-quoted JavaScript string for ASCII-only log patches containing backticks; reserve `String.raw` for TeX patches without template delimiters.

### 2026-08-27 Keep TeX escapes out of ripgrep regexes

- [TRAP] A routed proof search placed TeX fragments such as `\\le` in an `rg` regular expression, and ripgrep rejected the unsupported escape before searching.
- [WORKS] Search TeX prose with fixed strings or simple textual anchors; use multiple `-e` patterns when several concepts are needed.

### 2026-08-27 Distinguish the ordered wedge reader from exterior projection

- [TRAP] An intermediate proof treated the characteristic-two exterior quotient as if it agreed with the production reader on an antisymmetric tensor; the quotient sends `u tensor v - v tensor u` to zero modulo two, while the reader retains one ordered-half coefficient.
- [WORKS] Define the source-shortlex ordered-half map explicitly on intermediate tensors. Never use quotient-action invariance to move it across transport; retain the transport and order-reversal section-cocycle terms as one coupled package.

### 2026-08-27 Do not mix transported-section representations

- [TRAP] Combining the transported-old-order tensor with the full section defect double-counts the shortlex order change and falsely removes the differentiated inversion remainder.
- [WORKS] Use exactly one complete representation: transported-old-order plus one-vertex transport, or canonical-new-order plus the full section defect. The pairwise ordered-reader identity converts between them but does not cancel the inversion remainder from the actual derivative.

### 2026-08-27 Pair inversion actions before comparing powered words

- [WORKS] The pure shortlex-inversion term has an exact permutation cocycle with no quotient-section residual. Pair positive and negative occurrences through \(m=q_-q_+^{-1}\) before expanding old--new comparisons.
- [WORKS] Reduce collision-aggregated old and new currents modulo two before building comparator families; if both endpoint states are singletons, the entire inversion derivative is zero for every occurrence action.
- [WORKS] After relative-action pairing, use the fixed multiplier's length-displacement bound before invoking a powered comparator. Here the inversion bands are narrower than one 24-letter core step, so a formal old-prefix sum localizes to at most two affine boundary rays.
- [TRAP] A common-phase schema's `fixed_0` block is not necessarily a reduced prefix of the instantiated canonical word; an initial `tT` can cancel against itself or the following blocks. Never infer first-letter classes blockwise.
- [WORKS] Use the fully reduced canonical label records and their protected pump for first-letter or length claims; keep pre-collision rows that belong to the cumulative old chain even when they cancel from a one-step increment.

### 2026-08-27 Separate the powered-block seed from stable levels

- [TRAP] A uniform protected-block table can fail at level zero because a common root-edge collision changes one canonical row, even when that seed cancels from the two-step derivative.
- [WORKS] State stable block data only on the proved range and isolate the fixed seed before taking powered differences.

### 2026-08-27 Keep repeated-block covariance on the correct side

- [TRAP] Consecutive powered blocks do not generally give a common left translate of position-dependent module heads; moving the powered endpoint past their left prefixes silently conjugates by different elements.
- [WORKS] Normalize each local edge pattern first, retain its one-sided boundary, and use only the actual finite-action order for level periodicity.

### 2026-08-27 Evaluate finite readouts only after symbolic compression

- [WORKS] Once an unbounded inversion sum is reduced to a fixed local orbit function, evaluate the pinned permutation actions by residue tables rather than reopening a source census.
- [WORKS] Before individual substitution, split off structural rows such as \(\rho(c)=1\); here they force \(D=1\), \(\delta=A\), and constant block weights.
- [TRAP] In JavaScript `apply_patch` strings, double every TeX backslash, including command prefixes and spacing commands; an undoubled `\,` or `\r` escape can silently corrupt the rendered formula.

### 2026-08-27 Normalize inversion against the preimage side

- [TRAP] For an inversion kernel \(m=g^{-1}\) applied to \(gx\), measure cancellation from the preimage \(x\) against \(g^{-1}\); measuring the already translated word against \(g\) reverses the factor side.
- [WORKS] A full cancellation-depth signature needs one letter beyond the multiplier length, or an explicit next-letter/end flag, to decide the first surviving lexical comparison.

### 2026-08-27 Preserve canonical powered-schema factor order

- [TRAP] Applying an absorbed fixed block on the wrong side of the powered core changes finite-action phases and can flip a residue table even when lengths remain correct.
- [SUPERSEDED] Evaluate canonical schemas only in the stored order fixed-zero, fixed-one, powered core, fixed-two; index a terminal row as \(z_h\) before using the shifted row \(z_{h+1}\) in a seam.
- [TRAP] A schema exponent is not automatically its physical level: the old terminal-z schema has exponent offset zero but already contains one fixed \(P_*\)-block, so exponent \(h\) is physical \(z_{h+1}\). Shifting the residue again double-counts the base block.
- [WORKS] Bind physical level to the full source exponent plus absorbed fixed blocks before applying any seam index; audit old and new schemas separately.
- [TRAP] Protected cancellation depths need not extend to level zero. Here the stable \(P_*\) rows cancel all nine letters of \(B\), but the seed rows have distinct direct forms and transformed lengths.
- [WORKS] Evaluate an exceptional seed from its literal canonical labels and tie lexicography; never extrapolate a protected depth table backward.
- [WORKS] Merge semantic connector/powered collisions before ranking inversion pairs. Here terminal \(w_{3,i}\) is exactly powered row \(f_{i+1}\); treating both as separate would create spurious comparisons.

### 2026-08-27 Give project imports an explicit root in scratch replays

- [TRAP] Running a project-relative Python replay from `.scratch/` made that directory `sys.path[0]`, so imports from `experiments` failed before producing any mathematical output.
- [WORKS] Invoke scratch replays through the proof guard with `env PYTHONPATH=. python3 ...`, or insert the resolved project root explicitly before importing repository modules.

### 2026-08-27 Keep checkpoint verification commands separate

- [TRAP] A checkpoint inspection chained `git diff --cached --check`, `git status`, and `git diff --cached --stat` with shell control operators even though each command was independently meaningful.
- [WORKS] Run each checkpoint verification in its own tool call; do not use `&&`, `||`, or command separators to bundle status and diff checks.

### 2026-08-27 Do not probe powered syndromes through the full crossed AST

- [TRAP] Even a single low-index `symbolic_syndrome(anchored_direction(i,i))` evaluation did not finish inside the 30-second proof guard, so direct AST sampling is slower than the compressed theory route and yields no useful preflight evidence.
- [WORKS] Evaluate the crossed derivative only after decomposing it into finite-action local phases and the already compressed inversion source; do not enumerate full powered tensors as a diagnostic.

### 2026-08-27 Do not infer ordered-half periods from projected linear states

- [TRAP] Periodicity of projected old and increment vectors does not determine an ordered profile `H(u,v)=sum_(x<y) u_x v_y w(x,y)`: source shortlex is applied before finite projection, and an exterior quotient or finite point matrix can erase the orientation retained by the ordered-half reader.
- [WORKS] Before promoting a finite-state recurrence for a crossed tensor readout, list every product, inverse, and section-defect ordered profile and either pair it globally into proved `b/q` expressions or include it explicitly in the protected comparator catalog.
- [TRAP] A fixed-side bound proved for powered leaf schemas does not automatically cover product-node quotients, section-defect currents `o_(q,r)`, or inherited outer AST transports.
- [WORKS] For a numerical comparator onset, expand the full residual AST, bind its total unreduced literal mass, and add explicit node-quotient, defect-support, and composed-ancestor allowances before substituting the uniform fixed-side bound.
- [TRAP] Bounding fixed-side length does not preserve the nominal powered exponent after normalization: a fixed multiplier can cancel complete repeated cores.
- [WORKS] Unless every multiplier's cancellation depth is proved exactly, retain a two-sided raw length allowance in the moving-cut and finite-old separation bounds; here that allowance is \(4L+1\).

### 2026-08-27 Flatten recursive crossed coordinates at the literal stream

- [TRAP] Expanding a typed crossed coordinate node by node can make fixed quotient-section and ancestor transports look like independent moving comparator legs, even though they are bookkeeping for one associative literal stream.
- [WORKS] On a zero-linear residual endpoint, flatten the exact kernel stream into coordinate-specific one-token tensors plus chronological central-label cross terms. Keep `varrho_k(p)=W_k(T_p)` for each covector; never substitute the scalar full-wedge raw-mirror weight.
- [WORKS] After literal flattening, bound only the central-label schemas on moving comparison legs and control local one-token weights through the separate raw pump.
- [TRAP] Fixed literal and quotient-section atoms have zero toggle but still pair with moving shell atoms through the old--new term; local one-token compression does not remove that fixed comparison leg.
- [WORKS] Bound the complete fixed event stream separately before shrinking a comparator bound; here its unreduced mass is 216, below the leaf bound 372.

### 2026-08-27 Keep the AK3 continuation theory-first

- [TRAP] Launching broad local evaluations before exhausting the finite-action identities obscures the smallest proof obligation and conflicts with the user's requested theory-first continuation.
- [WORKS] First specialize the exact literal-stream theorem to one separating coordinate, prove symbolic covariance term by term, and run a guarded finite evaluator only if a genuinely irreducible bounded residue remains.
- [TRAP] TeX command fragments such as `gamma` with backslash syntax still fail inside an `rg` regular expression; an existing general warning is not a substitute for using the safe command form.
- [WORKS] Search TeX-heavy files with plain textual anchors or fixed-string patterns, never TeX control sequences in the regex.
- [TRAP] The first coordinate-four theorem patch repeated the known JavaScript escape failure and rendered the intended thin-space command as a plain comma.
- [WORKS] Use a raw patch string for every TeX-bearing proof edit and inspect each newly inserted display before staging.
- [TRAP] The coordinate-four checkpoint inspection again bundled two independent Git reads in one multiline shell command.
- [WORKS] Treat newlines as command chaining too; issue one shell command per tool call during checkpoint verification.
- [TRAP] A raw JavaScript patch template failed twice because TeX-style double-backtick quotation inside the Markdown prematurely terminated the template.
- [WORKS] Raw patch strings protect backslashes, not template delimiters; use plain prose quotation inside them and inspect the entire payload before retrying.

### 2026-08-27 Keep chord endpoints typed before prefix sweeps

- [TRAP] A transported chord label is not the module coordinate that orders an endpoint inside its occurrence; equal labels at two occurrences can come from different coordinates.
- [WORKS] Define occurrence-local prefixes at the actual module coordinate and apply the occurrence transport only to its finite color.
- [TRAP] The set-theoretic union of two matchings is not a matching of the symmetric-difference support when atomic copies cancel.
- [WORKS] Use bilinearity to evaluate the two matchings separately, or splice through canceled copies and discard alternating cycles.
- [TRAP] Pairing shifted endpoints cancels the chronology prefix strictly before the endpoint occurrence, not fixed correction or base atoms interleaved inside that occurrence.
- [WORKS] Retain every within-occurrence atom in the local color-prefix difference and cancel only the external prefix and complete intervening blocks.

### 2026-08-27 Do not apply a full chord cancellation to local pieces

- [TRAP] The complete shell self-prefix is zero by crossing double-counting, but its proof uses relative-order and cross-occurrence terms from the full chronology ledger. After corresponding shifted chords cancel the external occurrence prefixes, those terms are no longer present in the occurrence-local rows.
- [WORKS] Keep either the complete block expansion, including all relative kernels and enclosed occurrence packages, or the exact 48-row local-prefix formula. Never subtract a full-ledger zero termwise from only one decomposition's local pieces.
- [TRAP] In a shared worktree, another process switched from `codex/proofs` to `claude/results-collection-org-wmvpxu`, making the requested proof path appear missing.
- [WORKS] Before diagnosing a missing tracked proof file, check the active branch. Restore the user-requested branch only after confirming the other branch has no tracked changes, and leave its untracked artifacts untouched.

### 2026-08-27 Separate a flawed proof from a valid local cancellation

- [TRAP] After rejecting the global chord-crossing justification, the first retraction also rejected the shifted-prefix identity itself. That overcorrected: the added prefix cancels independently inside each positive/reverse-negative occurrence pair.
- [WORKS] When a global cancellation is mistyped, re-expand the exact surviving local formula before retracting its conclusion. Here invariance and symmetry make every unordered local shell pair occur twice, including the three slot-zero polarity pairs.
- [TRAP] A concurrent Git process left a zero-byte `.git/index.lock` and blocked the urgent correction checkpoint.
- [WORKS] Before removing an index lock, inspect it and confirm with `lsof` that no process holds it. Remove only the stale lock, then retry the exact staged paths.

### 2026-08-27 Evaluate natural-side seams after local pairing

- [WORKS] Once the local shell prefix is moved to the natural side, group every endpoint sweep by collision-aggregated polarity pair. The positive and reverse-negative orders produce the finite projected bilinear form directly.
- [WORKS] If the projected shell is index-independent and consecutive old masks differ by that shell, the two sweeps collapse to the alternating self-pair. Here this proves the full coordinate-four old-shell polarization zero for every index.
- [TRAP] A LaTeX plus sign at the start of an added patch line was consumed as the patch marker, leaving two adjacent bilinear terms with no operator.
- [WORKS] When an inserted mathematical line must begin with a plus sign, prefix it with an empty TeX group such as `{}` and verify the rendered equation immediately.

### 2026-08-27 Preserve TeX delimiters in JavaScript patch payloads

- [TRAP] Two proof replacements used ordinary JavaScript strings, which consumed the backslashes in TeX delimiters before `apply_patch` matched the file.
- [WORKS] Use `String.raw` for TeX-heavy patch payloads that contain no Markdown backticks, then inspect the exact diff before staging.
- [TRAP] A later promise-ledger patch ignored the backtick exclusion and failed at JavaScript parsing before `apply_patch` ran.
- [WORKS] Split TeX-heavy edits at inline-code lines so raw templates never contain a Markdown backtick; patch the intervening plain-text fragment separately.

### 2026-08-27 Do not bundle pre-checkpoint reads

- [TRAP] The relative-displacement checkpoint again combined branch, status, diff, and time reads with shell control operators despite the existing separate-command rule.
- [WORKS] Issue branch, status, diff-check, and timestamp as separate tool calls before every checkpoint; latency does not justify a noisy bundled command.

### 2026-08-27 Keep the ordered half inside relative-displacement quotients

- [TRAP] A proposed mixed `Xi` formula applied diagonal invariance to individual transported section tensors. Homogeneity then forced the whole Hessian to vanish, contradicting the proved nonzero anchored full-wedge polarization.
- [WORKS] Apply the relative-displacement histogram only after the final tensor is diagonal-free and antisymmetric. On intermediate tensors retain the source-shortlex ordered-half reader; local terms cancel under second polarization, but within-occurrence and external ordered cuts survive.
- [TRAP] A ledger wording repair used an ordinary JavaScript patch string whose unchanged context still contained a TeX command, so matching failed before the intended plain-text edit.
- [WORKS] When only prose changes, exclude every TeX-bearing context line from the patch and match the smallest plain-text line alone.

### 2026-08-27 Keep checkpoint patches atomic

- [TRAP] A proof-and-log patch failed because its context split a wrapped sentence incorrectly; the retry then failed because an empty third-file hunk invalidated the whole patch.
- [WORKS] Patch the proof and log only after reading their exact insertion anchors. Never include a placeholder file hunk; make lesson updates a separate patch and read them back.
- [TRAP] A follow-up retry put a Markdown-code log line inside a `String.raw` template, causing JavaScript parsing to fail exactly as the earlier lesson warned.
- [WORKS] Split raw TeX proof patches from ordinary-string Markdown-log patches before calling `apply_patch`; never mix backticks into the raw template.

### 2026-08-27 Type group-ring gauges on the permutation module

- [TRAP] The first Fox--Hessian gauge statement wrote the homogeneous kernel as `ker(L:R^5->R)`, although correction currents live in `M=Z[Q/<c>]`; this made the later forest-source bijection land in a different module than the displayed kernel.
- [WORKS] Keep matrix entries and gauges over `R=Z[Q]`, but type their action and homogeneous kernel on `M^5`. Before composing algebraic stages, verify domain, codomain, and support category explicitly.
- [TRAP] A candidate free-bucket set listed only one of the two polarized direction products and did not exclude trivial or inversion-fixed double cosets.
- [WORKS] When converting a symmetric polarization into an oriented-bucket candidate ledger, state both ordered direction terms and impose the exact free-bucket type conditions inside the set.

### 2026-08-27 Keep agent and process allocation lean

- [WORKS] Before continuing a long theory session, inspect proof/search processes and stop only runs proved stale; do not kill unrelated editor or workspace services.
- [WORKS] Use Terra or Luna for naive, mechanical, or implementation subtasks. Reserve Sol review for substantive AK3 theorem and soundness work.
