### 2026-08-29 Treat requested audits as preventive controls

[WORKS] When the user labels a soundness check preventive rather than a defect, add the control and report its outcome without rewriting an already verified theorem as invalid. Promote it to a correction only if the control actually fails.

### 2026-08-29 Move off a shared checkout immediately after a branch switch

[TRAP] Another process switched the main checkout from `codex/proofs` to an unrelated branch during a proof turn.

[WORKS] If the tracked tree is clean, restore `codex/proofs`, create the project-local `.claude/worktrees/codex-proofs` worktree, leave the shared checkout on its foreign branch, and perform all further proof edits, commits, and pushes from the isolated worktree. Never touch the unrelated duplicate files.

### 2026-08-29 Guard invocation and cache paths change in an isolated worktree

[TRAP] Executing `scripts/run_proof_guarded.py` directly failed because it is not executable. The first isolated offline pytest launch then failed because the new worktree-local UV cache did not contain pytest.

[WORKS] Invoke the guard with `python3 scripts/run_proof_guarded.py`. From `.claude/worktrees/codex-proofs`, reuse the project cache through `UV_CACHE_DIR=../../../.scratch/uv-cache` and keep bytecode in the worktree-local `.scratch/pycache`.

### 2026-08-29 A failed reviewer launch is not a proof review

[TRAP] The Sol xhigh reviewer returned a usage-limit error before evaluating the two-live omission normalization.

[WORKS] Do not count an infrastructure failure as APPROVE or REVISE. Preserve the exact theorem and focused certificate, retry a read-only review when capacity returns, and do not promote the checkpoint to a final AK3 claim without the required review.

### 2026-08-29 Re-read the exact post-patch line before a cleanup edit

[TRAP] A cleanup patch mixed a harmless no-op anchor with a TeX spacing line and failed exact verification.

[WORKS] After inserting a long theorem section, read the literal five-line span around each lint hit and patch only that exact line. Keep unrelated no-op anchors out of cleanup patches.

### 2026-08-29 Distinguish staged word audits from one linked proof object

[TRAP] Separate exact assertions for the Magnus, HNN, endomorphism, and Fox stages were described as one mechanically derived source-to-Fox chain even though two alphabet transitions were not executable rewrites of the preceding object.

[WORKS] State precisely whether a checker links every transition or independently verifies each displayed stage. For simultaneous ambient automorphisms, cite the every-rank theorem and record balanced triviality at the exact arity before applying it.

### 2026-08-29 Replace a cyclic-class representative before the next map

[TRAP] A trefoil-return replay proved `xTXTx` and `txtXX` were in the same inversion/conjugacy class but then applied the next ambient map to the old literal row, producing `xxTTx` instead of `ttXXX`.

[WORKS] When an AC1/AC3 normalization selects a new literal representative, assign that representative in the replay before applying the next simultaneous automorphism. Equality of cyclic classes does not mutate the stored word.

### 2026-08-29 Do not promote stable ambient transport to classical AC

[TRAP] A hostile review correctly checked every word in the trefoil return but then asserted an uncited classical ambient-automorphism principle, contradicting the repository's explicit stable-only boundary.

[WORKS] When review advice conflicts with a proved scope boundary, require the exact theorem and re-review the conflict. Here only the fixed-trefoil donor exchange is classical; simultaneous Nielsen basis transports use Proposition 3.3 and make the full return stable, not classical.

### 2026-08-29 Do not substitute ignored duplicate scratch certificates

[TRAP] The isolated worktree lacks the ignored canonical MMS02 relation-lift scratch scripts, and two guarded launches failed: one from a duplicated relative path and one on the missing canonical script. The shared checkout contains only unrelated `* 2.py` duplicates, which are outside the authorized staging and cleanup scope.

[WORKS] Do not rerun the unchanged command or import a duplicate scratch file. Recover the needed move or coefficient data from tracked proof artifacts, reachable Git objects, or the supplied full dump; otherwise restate the certificate in a new tracked focused checker whose inputs are explicit and reviewable.

### 2026-08-29 Separate solver discovery from dependency-free certificate replay

[TRAP] A direct rank-three Whitehead import failed first on missing NumPy and then on missing Numba in isolated runtimes. Adding offline packages could not resolve uncached wheels.

[WORKS] Use the configured bundled Python only for one guarded discovery run, with an import-only Numba decorator stub when the called proof code never executes Numba kernels. Persist the mathematical result through a dependency-free checker that replays the strict Whitehead descent and exhausts the complete 90-map endpoint table; do not make the final certificate depend on the stub.

### 2026-08-29 Never parallelize proof-guard invocations

[TRAP] Launching focused pytest and `py_compile` under the proof guard in one parallel batch made the second command refuse the active singleton lock as unreadable.

[WORKS] Run proof-guarded commands serially. Parallelize only read-only lint, diff, and text scans that do not acquire the guard; after a refusal, let the first guarded process finish and rerun the second command once.

### 2026-08-29 Do not enumerate cyclic products on the long Tpub SLP replay

[TRAP] Exhausting all rotation pairs after replaying the 134-move `u -> v` substitution exceeded the 60-second proof guard before producing a checkpoint.

[WORKS] Do not rerun or enlarge that sweep. Retain the exact replay lengths and direct cancellations only as scratch evidence, then return to the relation-lift DAG, Whitehead graph cuts, or another symbolic construction whose terminal obligation can be stated before execution.

### 2026-08-29 Separate a distinguished endpoint pivot from a forced one

[TRAP] A common-suffix cancellation was initially called the only pivot forced by the endpoint shape, and its legal row macro was mislabeled as AC2 even though this repository numbers row multiplication as AC1 and inversion as AC2.

[WORKS] Ground a distinguished pivot with exact free-word reconstruction, state its complete AC1--AC2 macro, and leave every untested endpoint pair explicitly unclassified. Exponent-matrix shape alone does not make a Nielsen pivot unique.
