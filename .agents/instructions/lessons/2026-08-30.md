# Lessons learned — 30 August 2026

### 2026-08-30 Count convergence checkpoints by ancestry

[TRAP] Reading a newest-first path log in the wrong direction made the remote-ledger budget appear exhausted even though the budget rule was committed after Sections 3.99--3.106.

[WORKS] Locate the commit that introduces a convergence rule, then count only descendant commits which touch its scoped sections.  Earlier checkpoints never consume a prospective budget.

### 2026-08-30 Scope lint around preserved debt

[TRAP] Whole-file Ruff reported 27 pre-existing E701 findings in a certificate whose new test was already clean, so an unqualified lint result did not isolate the current patch.

[WORKS] Preserve unrelated formatting debt and rerun the same file with only the known pre-existing rule excluded.  Report both the baseline finding and the scoped passing result; do not reformat proof infrastructure opportunistically.

### 2026-08-30 Keep rectangular relation matrices handed

[TRAP] For a left relation module, a coefficient matrix `C` with `e=C*b` was initially treated as a left-linear map by column multiplication.  Over a noncommutative group ring that column map is right-linear, so the stated left-module cokernel was ill-handed.

[WORKS] Use row vectors and right multiplication `rho_C(x)=x*C` to obtain the left-module split surjection from `D*C=I`.  Complete a rectangular generating system through its free stabilized kernel, and use only the elementary/diagonal move dictionary from square-system theorems.

### 2026-08-30 Legal AC4 blocks preserve the rectangular kernel

[TRAP] Treating a stable `U direct-sum I_r` factorization as automatically ordered like a legally stabilized residual system placed the old zero/complement coordinate before the new AC4 basis rows, so its displayed first columns were not `diag(C,I_r)`.

[WORKS] AC4 adds a collapsible 1--2 pair and an identity block to both module basis and residual rows, preserving the rectangular kernel.  Move the old zero coordinate past new legal rows with an explicit tame permutation before reading the first columns; legal square completion is then equivalent to freeness of that unchanged rank-one kernel.

### 2026-08-30 Pin the proof-checker Python runtime

[TRAP] The host `python3` resolved to an interpreter too old to import a focused proof certificate using PEP 604 annotations, failing at `str | None` before any proof logic ran.

[WORKS] For this dependency-free certificate, invoke named test functions directly under the guarded explicit interpreter `/Users/avigyapaudel/.local/bin/python3.11`; that interpreter parses the module but does not contain pytest.  Use the project-local isolated `uv` recipe from the 2026-08-28 lessons only when pytest collection itself is required, and never assume the shell's `python3` is compatible.

[TRAP] The pair minimizer's cyclic canonicalizer does not accept an empty word, so `(relator, "")` is not a valid singleton adapter.

[WORKS] For a one-word Whitehead check, reuse the certified automorphism and word-canonicalization primitives but minimize the singleton directly; never encode a missing second word as an empty cyclic word.

### 2026-08-30 Double-escape TeX in JavaScript patch strings

[TRAP] A single backslash before a TeX spacing command inside the JavaScript source passed to `apply_patch` was consumed by JavaScript, leaving the malformed superscript `^{,P}` in the Markdown source.

[WORKS] Double every TeX backslash in JavaScript patch strings and read back the exact edited equation.  Prefer notation that needs no spacing escape when the spacing is nonessential.

### 2026-08-30 Search literal TeX with fixed strings

[TRAP] A regular-expression search for a TeX fragment containing braces failed before inspecting the proof because the unescaped fragment was parsed as a repetition expression.

[WORKS] Use separate fixed-string `rg -F -e ...` patterns for literal TeX tokens; do not place raw TeX braces in an ad hoc regular expression.

### 2026-08-30 Reject control bytes after generated patches

[TRAP] A generated Markdown patch briefly inserted terminal emphasis control bytes around a TeX symbol even though no terminal formatting belonged in the source.

[WORKS] After every generated documentation patch, scan the touched file for bytes below ASCII space other than tab, newline, and carriage return before staging.  Read back the edited paragraph separately from the ordinary diff check.

### 2026-08-30 Endpoint localization is cell-relative

[TRAP] A terminal-neighborhood theorem for two tokens inside one anchored cell was promoted to a support-independent multiplier alphabet for mixed edges between two arbitrary non-target cells.  This made a cycle-specific Bass--Serre estimate look uniformly linear across the complete mixed graph.

[WORKS] Track the cell whose radius supplies every endpoint-localization hypothesis.  For a complete mixed graph, keep actual edge-dependent multiplier families unless a separate global localization lemma is proved.  Branch injectivity survives for each fixed multiplier, but the number of branches and their union's local degree need not be uniform.

### 2026-08-30 Invoke the guarded runner through its interpreter

[TRAP] The proof guard is tracked without an executable bit, so invoking `scripts/run_proof_guarded.py` directly failed before the focused test began.

[WORKS] Launch short proof checks as `/Users/avigyapaudel/.local/bin/python3.11 scripts/run_proof_guarded.py ...`; a permission error before guard startup is an environment failure, not a mathematical result.

### 2026-08-30 Keep edge indices distinct from class keys

[TRAP] A dense link-inventory one-liner reused one variable for an integer edge index and a parallel-class tuple, then indexed a list with the tuple and failed before printing the class table.

[WORKS] Name integer edge identifiers and class keys separately in finite link audits.  Prefer a short checked certificate over a compressed one-liner once the result will enter a proof document.

### 2026-08-30 Preserve raw cyclic seams in Neuwirth corridors

[TRAP] A branch summary said every Appendix F path state had loopless `K4` or `K4-e` support, but the raw exact states at indices 23 and 24 each contain one seam loop.  Cyclically reducing those words erases the loop and changes the word-realized presentation complex being tested.

[WORKS] Replay AC paths with only the reductions authorized by each move, classify every emitted exact word before any cyclic normalization, and fail closed into the proved one-loop solver when a seam loop appears.  Keep cyclically reduced companion rows separate from exact-complex claims.

### 2026-08-30 Pin every HNN base segment before reducing

[TRAP] A manual HNN pinch concatenation swapped the first base segment `yxY` to `xyX`, producing a false reduced tail and false Whitehead floors.

[WORKS] Pin the raw stable-letter skeleton, list every pinch explicitly, and pass the resulting base word through an independent stack reduction before running Whitehead minimization.  Cross-check the final floors with a separately implemented automorphism engine.

### 2026-08-30 The autcanon public API is pair-only

[TRAP] Calling `aut_min_len((word,))` raised `IndexError` because `experiments/equivalence_classes/lib/autcanon.py` indexes exactly two components; a one-tuple is not a singleton interface.

[WORKS] For a one-word cross-check, use the duplicated pair `(word,word)`: a common automorphism gives two identical cyclic images, so its complete pair floor is exactly twice the singleton floor.  Never pass a one-tuple or an empty second component to the pair API.

### 2026-08-30 Eliminating an extremal Magnus generator keeps its shift

[TRAP] Solving a Magnus base relator for the unique extremal generator `y_2` was incorrectly treated as deleting the relation `x y_1 x^-1 = y_2`.  This shrank both associated subgroups from rank four to rank three and created a false abelian contradiction in a Collins conjugacy equation.

[WORKS] After eliminating an extremal base generator, substitute its solved word into the last stable-letter relation.  Recompute both associated subgroups and the full induced abelian map before using any twisted-conjugacy obstruction; elimination changes the target word, not the rank of the associated subgroup.

### 2026-08-30 Do not use the bundled workspace Python for Numba searches

[TRAP] The primary workspace Python imported NumPy but raised `ModuleNotFoundError: No module named 'numba'`; it cannot run `experiments/equivalence_classes/search/aut_search.py`.  This repeated the already documented isolated-runtime dependency boundary before any search state was explored.

[WORKS] Treat the Aut-class search stack as unavailable unless a pre-existing project interpreter is first shown, by a guarded import-only check, to contain both NumPy and Numba.  Do not install dependencies or reinterpret an import failure as search evidence.

### 2026-08-30 Scope lint around legacy E701 debt

[TRAP] A whole-file Ruff check on `tests/stable_ac/test_ak3_mms02_relation_lift_certificate.py` reported 27 pre-existing E701 one-line-suite violations outside the new finite-quotient evaluator, obscuring whether the added code introduced a lint defect.

[WORKS] Preserve the legacy proof DAG formatting and rerun Ruff with only E701 ignored; require every other rule to pass, then inspect the patch itself for new one-line suites.  Do not turn a focused proof checkpoint into an unrelated formatting rewrite.

### 2026-08-30 Left relation-module labels update on the right

[TRAP] In the finite-quotient evaluator for the convention `g[r]=[g^-1 r g]`, outer conjugation by `q` was implemented as the label update `h -> q^-1 h`.  This agrees on a basis leaf but reverses nested noncommuting conjugators and produced a false 59-term image for `delta`.

[WORKS] A coefficient labelled `h` represents `[h^-1 r h]`, so conjugating its representative by `q` updates the label to `h q^-1`.  Pin a nested noncommuting control such as `x(y B y^-1)x^-1`, whose label is `y^-1 x^-1`, before trusting any noncommutative group-ring aggregation.

### 2026-08-30 Substituted endpoint coordinates require the chain rule

[TRAP] Passing `e_b_eq_one.evidence` directly to the finite-quotient evidence evaluator failed on a `subst-evidence` node.  Unlike the already-expanded tag residual, `[E_B]` contains the proof-DAG substitution of `H_B` for `v`; treating it as an ordinary product would either fail or silently lose quotient-prefix actions.

[WORKS] Reconstruct `[E_B]` by the same decomposition as the certified cyclic calculation: add the base evidence for `K=u^-1`, differentiate the `v -> H_B` substitution, and transport by the outer `u`.  With labels for `g[r]=[g^-1 r g]`, a product-child derivative right-multiplies by the inverse preceding-prefix value, an inverse right-multiplies by the child value with sign minus, and fixed conjugation right-multiplies by the inverse conjugator.  Pin separate product, inverse, and conjugation controls before using the coordinate in an ideal calculation.

### 2026-08-30 Donor-factor replay order

[TRAP] In the terminal trefoil transfer probe, a defect
`D=f_1...f_n=row*target^-1` was compiled by iterating the factors backward
while also prepending their inverses.  Those two reversals canceled, producing
the wrong left multiplier and thousand-letter source rows instead of the
standard pair.

[WORKS] To realize `target=D^-1*row` by successive left multiplications,
iterate `f_1,...,f_n` in the displayed forward order and prepend each
`f_i^-1`; the accumulated multiplier is then `f_n^-1...f_1^-1=D^-1`.
Always assert the known source endpoint before inspecting a transported target.

### 2026-08-30 Stable reduction is not orbit equivalence

[TRAP] After proving the terminal rank-two source pair AC-trivial, the first
draft said that AC-triviality of the cleaned target was equivalent to closure
of the stabilized pair orbit (271).  The reduction to the rank-two pairs
(293) uses stabilization and destabilization; a path in the larger tuple may
use the stabilizing row and need not descend to a rank-two path.

[WORKS] Preserve the direction of every stable reduction.  Here a rank-two
path in (293) lifts and is sufficient for (271), while closure of (271) does
not imply a rank-two path.  State the if-and-only-if only between the chosen
rank-two gate and the cleaned target presentation.

### 2026-08-30 Reuse the pinned dependency-free test entrypoint

[TRAP] A new focused standard-library certificate was sent to
`/Users/avigyapaudel/.local/bin/python3.11 -m pytest` even though the dated
lessons already record that this interpreter has no pytest module.

[WORKS] Search the dated interpreter lessons before launching a proof check.
For dependency-free certificates, import and call the named test functions
directly under the guarded explicit Python 3.11 interpreter; use the isolated
`uv run --with pytest` recipe only when pytest collection is itself required.

### 2026-08-30 Recount every descended HNN word from its pinned spelling

[TRAP] The one-step preimage
`QpQPPqpqPqpqqPq` was manually assigned abelianization `(-1,5)`, although
its exact letter count is `(-1,4)`.  The literal identity `phi(Q)=P` passed
before the incorrect secondary pin stopped the focused replay.

[WORKS] Derive the preimage abelianization from the exact reduced word and
cross-check it through the endomorphism matrix.  Here `(-1,4)` maps to
`(0,-1+2*4)=(0,7)`, the independently pinned abelianization of `P`.

### 2026-08-30 Do not place Markdown backticks inside a raw JS template

[TRAP] A `String.raw` patch wrapper contained Markdown code backticks from a
checkpoint log, so JavaScript terminated the template before `apply_patch`
could run.

[WORKS] Use an ordinary quoted JavaScript string with explicit newlines for
patches containing Markdown backticks.  Reserve raw templates for patches
whose content has no unescaped template delimiters.

### 2026-08-30 Inspect the index after an ignored-path staging error

[TRAP] A multi-path `git add` returned status one and warned that the tracked
proof under `literature/` was ignored, but it had already staged every exact
requested path, including that proof.  Retrying with force would have added
no evidence and risked obscuring the actual index state.

[WORKS] After any nonzero staging command, inspect both `git status --short`
and `git diff --cached --name-status`.  If the exact intended set is already
staged, do not rerun or broaden the command; stage only any later lesson
readback change.

### 2026-08-30 Split dependency-audit corrections by file

[TRAP] One patch combined four proved scope repairs in the period-two source
with three audit-summary updates.  A one-line wrap at the audit boundary
differed from the expected context, so `apply_patch` rejected every hunk and
made no change.

[WORKS] After a dependency audit finds a proof-scope defect, patch and read
back the theorem source first.  Patch the audit summary and convergence
accounting separately from their exact current seams so a stale prose wrap
cannot block the soundness correction.

### 2026-08-30 Patch module and test zip bounds separately

[TRAP] A correction for mismatched strict-zip bounds targeted the module and
its test but named only the module in one patch update.  The test-only context
could not exist there, so `apply_patch` rejected both otherwise valid edits.

[WORKS] When the same mechanical correction spans a new certificate and its
test, use one explicit update block per file.  Read the exact loop seam in
each file before changing strict zip bounds.

### 2026-08-30 Pin displayed witnesses independently of their finder

[TRAP] The cyclic-descent certificate dynamically selected the first legal
rotation/sign witness, and its test called the same finder again.  This pinned
the floor chain but did not independently authenticate the nine witness rows
printed in the proof table.

[WORKS] Store every displayed order, sign, rotation, retained-row, product-
length, and ambient-descent witness in an immutable expected constant.  Assert
the computed dataclasses equal that constant; use the finder only as a replay,
not as its own oracle.

### 2026-08-30 Anchor chronological log appends at the actual tail

[TRAP] A checkpoint-log patch used a repeated nonclaim sentence as context,
so `apply_patch` inserted the new dated section before an older checkpoint.

[WORKS] Read the literal final lines first and anchor dated checkpoint appends
to unique tail context.  Immediately verify the new heading is the final log
section before staging.

### 2026-08-30 Validate focused proof-command imports before launch

[TRAP] A hand-typed guarded one-liner split `FLOOR_PAIRS` with a space, so the
child exited at Python parse time and performed no proof check.

[WORKS] Copy exact import names from the read-back file and syntax-check the
command text before launching a guarded focused replay.

### 2026-08-30 Use singleton-capable Whitehead entrypoints for one word

[TRAP] `autcanon.aut_min_len` indexes two rows and raises `IndexError` on a
singleton tuple; it cannot certify primitivity of one candidate word.

[WORKS] Use `mms02_terminal_hnn_certificate.whitehead_minimum` for singleton
floors.  Reserve `aut_min_len` for two-row cross-checks unless its arity is
first verified.

### 2026-08-30 Derive class-two signs from the literal collector

[TRAP] A hand-derived class-two substitution used a commutator sign from a
different normal-order convention.  The focused certificate correctly
rejected the claimed relator image before theorem staging.

[WORKS] Pin the collector's normal order and multiplication law first, derive
the relator coordinate literally in that convention, and only then state the
elimination formula for the third generator.

### 2026-08-30 Double LaTeX escapes inside JavaScript patch strings

[TRAP] Inline LaTeX such as `\(R\)` and `\qquad` was passed with single
backslashes inside a JavaScript string.  JavaScript consumed them, leaving
plain parentheses and `qquad` in the proof read-back.

[WORKS] Double every LaTeX backslash in JavaScript patch source, then read
the entire inserted theorem back before testing or staging it.

### 2026-08-30 Preflight the guarded Python test runner

[TRAP] `/Users/avigyapaudel/.local/bin/python3.11` does not include `pytest`,
so a guarded focused invocation exited before collecting any certificate test.

[WORKS] Check the selected interpreter for the requested test module before
launch.  When `pytest` is absent, import and call the small focused test
functions directly under the same proof guard.

### 2026-08-30 Keep Markdown backticks out of JavaScript raw patch templates

[TRAP] A Markdown code span inside a `String.raw` template terminated the
JavaScript template before `apply_patch` ran; the proof edit was not applied.

[WORKS] Use plain prose or escaped delimiters inside raw patch templates, then
read back the target section before treating the edit as present.

### 2026-08-30 Obey the already-pinned dependency-free test entrypoint

[TRAP] The A5 homology certificate was again sent to the explicit Python 3.11
interpreter with `-m pytest`, repeating the same-day documented failure before
test collection.

[WORKS] Before every dependency-free proof check, search the current dated
lessons for the interpreter name.  Under the guard, import the focused test
module and invoke its named plain-assertion functions directly; do not probe
pytest again on this interpreter.

### 2026-08-30 Preserve the repository import root for scratch probes

[TRAP] Launching `.scratch/mms02_depth5_lift_probe.py` by file path set the
script directory as the import root and failed on the first `experiments`
package import, before any proof calculation ran.

[WORKS] Launch project-relative scratch probes under the guard through
`env PYTHONPATH=.`, keeping the repository root explicit.  Do this on the
first launch instead of repeating the already documented direct-script trap.

### 2026-08-30 Carry the full Tietze map into finite quotients

[TRAP] A first small-quotient probe correctly used the collapsed one-relator
but incorrectly identified original `z` with the surviving `z` generator.
The resulting apparent cyclic obstruction contradicted the already proved
abelianization and is discarded.

[WORKS] Evaluate every original generator through the complete automorphism
in (213) and only then delete the generator killed by `psi(B)=x`.  Pin all
three collapsed images before evaluating source, target, or a finite-quotient
skeleton.

### 2026-08-30 Keep guarded inline test commands syntactically flat

[TRAP] A nested formatted-print expression inside the JavaScript-wrapped
guard command failed at tool parsing before the test interpreter launched.

[WORKS] For small dependency-free suites, import the named test functions,
call them explicitly, and print only a numeric count.  This keeps shell,
JavaScript, and Python quoting boundaries independent.

### 2026-08-30 Re-read moving theorem-tail anchors before patching

[TRAP] A combined scope-list and terminal-tail patch used a stale sentence
boundary after the new Section 6.64 insertion, so `apply_patch` rejected the
whole edit without changing the file.

[WORKS] After inserting a long theorem section, locate the strict-nonclaim
item and final theorem paragraph again with `rg` and `tail`, then patch those
two anchors separately.

### 2026-08-30 Never combine summary and moving section-boundary patches

[TRAP] The Section 6.70 draft repeated the stale-boundary failure by trying
to patch the top summary and the pre-Section-7 insertion in one operation.
The patch was rejected atomically and no theorem text changed.

[WORKS] Treat the top summary, theorem insertion, strict-nonclaim list, and
terminal tail as four independent edits.  Re-read each anchor immediately
before its own patch.

### 2026-08-30 Reuse the pinned scratch import entrypoint

[TRAP] The all-A5 scratch probe repeated the already documented direct-file
launch and again failed before computation with `ModuleNotFoundError:
experiments`.

[WORKS] Before launching any project-relative scratch probe, search the
current dated lessons for `scratch` and use the pinned guarded entrypoint
`env PYTHONPATH=.` on the first attempt.

### 2026-08-30 Preserve inline LaTeX in JavaScript patches

[TRAP] A normal JavaScript patch string preserved doubled display commands
but consumed the single backslashes around inline LaTeX in the new Section
6.72, leaving plain parentheses until readback caught it.

[WORKS] Use a `String.raw` patch with no Markdown backticks for theorem prose
containing LaTeX, or double every inline delimiter.  Immediately inspect the
inserted section with both normal `sed` and `sed -n l` before continuing.

### 2026-08-30 Detect control bytes after escaped proof edits

[TRAP] `sed -n l` displayed a literal backspace byte as `\\b`, which looked
like a valid LaTeX command and survived two text-only repair attempts.

[WORKS] After any escaped proof edit, scan the entire document for C0 control
bytes and inspect suspicious lines with `od -tx1c`.  Repair the byte first,
then verify that each LaTeX delimiter contains exactly one backslash.
