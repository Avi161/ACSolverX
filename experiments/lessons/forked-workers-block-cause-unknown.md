# [2026-07-09] Forked workers block inside greedy_search — cause NOT yet established [TRAP]
Instrumentation localised the "silent heartbeat": `[hb] pres N started` printed for all 4 workers (so the pool, the `mp.Queue` and the main-thread drain were healthy), but `msgs_received` froze at 4 and `live=0` forever. The `start` message is enqueued *before* `greedy_search`, so the children were alive and then blocked at their first numba call — no worker ever reached its first 1024-node tick, which in debug (`first_after=0`) emits immediately. **So the 1M sweep was making ZERO search progress, not merely failing to report it.**
I hypothesised a numba-compiler deadlock (wandb.init() makes the parent multi-threaded; `mp.Pool` forks it; a child forked from a threaded parent can deadlock on a lock held at fork) and shipped a parent-side JIT warm-up. **That hypothesis is UNPROVEN: the hang did not reproduce locally** under threaded-parent + wiped numba cache + fork (completed in 4.1s). Cold compile is only 2.5s single-process, so "4× slow JIT" never explained 65s+ of silence either. Keep the warm-up regardless — it turns n_workers cold compiles into one — but do not claim it as the fix.
**Instrument, don't theorise:** `HEARTBEAT_DEBUG` now arms `faulthandler.dump_traceback_later(40, repeat=True, file=...)` in each worker, writing `hb_stack_<pid>.txt` (a worker's stderr is invisible in a notebook, so a file is the only channel); the first progress tick cancels it, so a healthy run leaves the files empty. Verified: a deliberately stuck forked worker dumps its exact blocked frame. **Rules:** when a bug appears only in the user's environment, get a stack from THAT environment rather than re-testing a clean local one; a fix that "makes it work locally" proves nothing when the failure never reproduced locally. Tagged queue messages (`start`/`sample`/`done`) are what made "worker never ran" separable from "worker alive but stuck" — instrument the boundary, don't guess across it.
