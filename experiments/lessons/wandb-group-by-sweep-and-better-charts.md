# [2026-07-09] W&B: group by SWEEP, not by date; and pres_id was never a useful x-axis [WORKS]
The project was one flat pile of runs because `group = f"greedy_baseline_{date}"` — every run started on the same day collapsed into one sidebar section regardless of dataset, cap, or subset — and `job_type` was a hardcoded constant with no tags and 6 config keys, so the runs table could not be filtered or grouped. The only chart was `solve_rate` vs `pres_id`, and **`pres_id` is just the presentation's line number in the data file**: a running average plotted against file order encodes nothing.
**Fix (new `experiments/wandb_tracking.py`; `run_baseline.py` keeps only thin call sites).** `group` = the sweep identity = `_run_prefix` minus budget and date (`ms640_solved-mrl24-cyc-all`), so a group's runs differ only by `node_budget` and the group view compares budgets for free. `job_type` stays the **algorithm family** (`greedy_baseline`) — `beam/` and `ppo_ac_s.py` will need that axis; budget lives in numeric `config.node_budget` (sorts correctly, unlike the string `"1M" < "50k"`), the `b50k` tag, and the display name. `config` is an **allowlist** (search knobs + `solver`/`n_workers`/`git_commit`/`hostname`); dumping the whole cfg would put `HEARTBEAT_*`/`GB_PER_PRES`/`DRIVE_OUT_DIR` in the runs table and re-create the mess. `id` (= the jsonl stem) is untouched, so resume still works; `name` is now a readable display string. On reattach `wandb.init` ignores a changed group/job_type, so the `resumed` tag is appended to `run.tags` **after** init.
**Charts.** Panel keys are namespaced (`profile/`, `difficulty/`, `hump/`, `dist/`, `perf/`, `unsolved/`, `rel/`, `run/`, `pres/`) so W&B auto-sections the workspace. The headline is **`profile/solve_rate_vs_budget`**: `solve_rate(b) = |{solved ∧ nodes_explored ≤ b}| / n_pres`. Because the search is deterministic and a budget only truncates it, this is **exact, not an approximation**, for every `b ≤ node_budget` — so one 50k run subsumes the whole `[1k..50k]` sweep and does *less* work (the sweep re-pops the 0→1k prefix five times). `summary.solve_rate_at_{1k,5k,…}` is read off the same curve, so the runs table alone compares budgets. Second: **`hump/path_profile`**, `L(t)=|r1|+|r2|` replayed from the stored `path_moves` — the Two-Hump curve, the only view that shows the search *lengthening* a relator to solve. Then difficulty-vs-`initial_length` (= `len(r1)+len(r2)`, derivable from any row, spans 7–25 on ms640), solved/unsolved `nodes_explored` histograms (`wandb.plot.scatter` has **no** color param in 0.28 — a categorical split must be two keys, not one colored panel), `unsolved/closest_approach_hist` (= `min_relator_length` over unsolved rows; it is a constant 2 for solved ones, so it is only meaningful there), and an enriched `tables/results`.
**Two live x-axes, never `step=`.** `run/*` (cumulative) keys on a new monotone `n_processed` seeded from `len(done)`; `pres/*` keys on `pres_id`, which `imap_unordered` delivers out of order — a step_metric tolerates that, `step=` would not.
**HIGH_SPEEDUP hazard the design had to absorb:** a solved presentation's row is written only after the pool tears down and a serial re-solve recovers its path. So `LiveLogger.on_result` (worker result arrives) accumulates `run/*` and `on_row` (row finally written) logs `pres/*` — accumulating in both would double-count `nodes_explored`. Same reason `time_seconds` means two different things on a heavy run (unsolved = in-worker heavy solve, solved = serial recovery re-solve), so `summary.timing_regime` reports `mixed(heavy)` and the timing panels say so.
**Correctness trap that the data happens to hide:** `hump_height = max_relator_length_expanded − initial_length` compares a *canonical* length against a *raw* one, and cyclic reduction can shorten the stored input. Measured: raw == canonical on **all 901 presentations** of `ms640_solved` + `ms_reps_unsolved` under both cyclic settings, so it never bites today — the baseline is still computed via `reduce_relator_nj` + `canonical_pair_nj` so it stays right for future data. **Rule:** when a derived metric mixes a stored value with a transformed one, verify they agree instead of assuming, and compute it the safe way regardless.
**Verified offline** (`WANDB_MODE="offline"`, no key): `tests/wandb_tracking_test.py` 15/15 (hand-computed ECDF/bucket/derive fixtures; a real ms640 solve replayed to assert `L(0) == canonical start` and `L(-1) == 2`) and `tests/wandb_offline_integration.py` (7 phases: identity + config-leak check, all 17 panels emitted from a real solved/unsolved mix, resume reattaches the same jsonl with a stable run id, an old-**dated** jsonl still resumes, the heavy path writes one row per pres_id, `cum_nodes` never double-counts). `_run_prefix`/`_resolve_paths`/`_subset_tag`/`_read_done` asserted byte-identical to `HEAD`. **wandb 0.28 offline writes no `files/config.yaml` or `wandb-summary.json`** (history lives in the binary `.wandb` log), so assert on the live `run` object and rebuild the panel dict from the jsonl instead of parsing files. **Not verified:** whether `prefix/` keys actually render as workspace sections — that is UI-side and needs one online run.
