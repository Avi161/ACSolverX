# [2026-07-09] Two real bugs in run_baseline.py, pinned as xfail(strict=True) [TRAP]
Found while mapping the runner for the test suite; NOT fixed (repo rule: do not modify existing code). Both live in `experiments/greedy_tests/test_known_gaps.py`, which asserts the *desired* behaviour and is `xfail(strict=True)` — so they turn RED the moment someone fixes the bug, prompting removal of the marker.
1. ~~`run_baseline.py:564` — `_w1, _w2 = todo[0][1], todo[0][2]` runs unconditionally inside `if high and n_workers > 1`. Re-running a **fully-resumed** heavy sweep (`n_todo == 0`) to confirm it is complete raises `IndexError` in the numba warm-up instead of printing "nothing to do". The crash precedes `ctx.Pool`, so no worker is ever spawned (hence it is a fast test).~~ **[FIXED 2026-07-09]** guard is now `if high and n_workers > 1 and jobs`; the strict xfail XPASSed exactly as designed, and the marker was deleted. The test remains as a plain regression test. Fixed alongside [heavy-mode-defers-solved-rows](heavy-mode-defers-solved-rows.md), which needed the same resume-with-nothing-to-search path to work.
2. `run_baseline.py:188` — `_read_done`'s `json.loads(ln)` is uncaught. A truncated trailing line — exactly what a Colab disconnect mid-write produces — crashes the resume that the jsonl exists to enable. Blank lines are skipped; a partial JSON object is not. **Still open** — deliberately left alone: one concern per change.
**Rule:** when a repo forbids touching existing code, `xfail(strict=True)` is the right home for a known defect — it documents the bug, keeps the suite green, and fails loudly once the bug is gone. A plain `skip` would rot silently. **The lifecycle works:** bug 1's marker turned RED the moment the fix landed, which is how the fixer learned to remove it.
