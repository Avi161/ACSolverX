# 4.1 — Phase 1 (Anchors) — Results

*Execution record for Phase 1 of `4.DETAILED_STEPS_DATA_CRAFTING.md`. Status: **complete, all
checks green.** CPU-only, numba-absent (`canon` shim). Run from repo root with `../.venv/bin/python`.*

## What ran

`scripts/build_anchors.py` supplied the short-hard / hard presentations the raw data lacks:
- **8 named anchor rows** → `data/anchors.jsonl` (new training rows; censored + one-sided hinge).
- **16-key search-time trap-set** → `data/ak_trap_set.json` (`{8 named} ∪ {8 in-data cousins}`).

## Results

**Named anchors (`tier=named`, `loss_type=hinge`, `bound_B=150` = Phase-0 `B_hard`, all ABSENT from
the archive — verified, so the eval is uncontaminated):**

| label | canonical key | total_len |
| --- | --- | --- |
| AK(3) | `YXYxyx\|YYYYxxx` | 13 |
| AK(4) | `YXYxyx\|YYYYYxxxx` | 15 |
| AK(5) | `YXYxyx\|YYYYYYxxxxx` | 17 |
| AK(6) | `YXYxyx\|YYYYYYYxxxxxx` | 19 |
| AK(7) | `YXYxyx\|YYYYYYYYxxxxxxx` | 21 |
| AK(8) | `YXYxyx\|YYYYYYYYYxxxxxxxx` | 23 |
| Length 14 #1 | `YYYXyyx\|YXXYxxx` | 14 |
| Length 14 #2 | `YYYXyyx\|YXXXyxx` | 14 |

AK(8) is the deliberate +1 over the lab dict (which lists AK(3)–AK(7)); cap = AK(8) keeps growth
headroom (r2 len 17 ≤ 24).

**In-data cousins (`group=short_hard`, the 8 censored rows with `total_len ≤ 13`):** these already
exist in `dot_archive.jsonl` — Phase 1 only **records their keys**, it does NOT add rows. 3 of the 8
carry AK(3)'s exact r1 `YXYxyx`. Keys: `YXXYx|YYYYXyX`(12), `YXXYx|YYYXyxyX`, `YXXYx|YYYYYXXX`,
`YXYXyX|YYXXXXX`, `YXYxyx|YXXYxxx`, `YXYxyx|YXyXYxx`, `YXYxyx|YYYxxxx`, `YYxxyX|YYXyxxx` (all 13).

**Finding-2 trap confirmed:** AK(3) `YXYxyx|YYYYxxx` is **absent** and is **distinct** from the
solved look-alike `YXyxYx|YYYYxxx` (present in the archive, d-o-t 12) — they differ only in r1. This
is exactly the case the anchors exist to make learnable.

## Where everything is

| artifact | path |
| --- | --- |
| named anchor rows (8) | `data/anchors.jsonl` |
| trap-set (16 keys + label map) | `data/ak_trap_set.json` |
| Phase 1 builder | `scripts/build_anchors.py` |
| Phase 1 tests | `scripts/test_phase1.py` |
| config path constants added | `scripts/dot_config.py` (`ANCHORS_JSONL`, `TRAP_SET_JSON`) |

## Did everything go right?

**Yes.** `python scripts/test_phase1.py` → `PHASE1 TESTS PASS` (exit 0), 40+ checks: anchor schema,
canon re-derivation == expected keys, all 8 named absent, trap-set = 16 distinct keys, named∩cousins
= ∅, cousins independently recomputed == `short_hard` group, Finding-2 distinctness, and
`bound_B == percentiles.json B_hard`. Two consecutive builds produced **byte-identical** outputs
(determinism). `git status` shows only the new files + the `dot_config.py` edit — no original data or
script modified.

## Notes for later phases

- **Cousins → TRAIN, not frozen.** The trap-set's `short_hard` group is a *tag instruction* for
  Phase 2 (tag those archive rows `group=short_hard` in place) and routes to **train** in Phase 7.
  Only `tier=named` goes to the frozen `counterexample_eval` — this is the USER-decided SUPERSEDES of
  source §5 ("named + cousins" → "named only").
- The trap-set is the carrier of the cousin tag-instruction (Phase 2 reads `groups.short_hard`) and
  the search-time monitor for `experiments/PLAN.md §8` / `experiments/training/PLAN.md §8`. Living
  set: novel basins get promoted in later.
- Anchors carry no `weight` / `n_paths` / `near_neighbour_count` yet — those are assigned in
  Phases 5/3 during the v2 archive build.
