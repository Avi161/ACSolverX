[pytest]
; Root tests/ holds the CI suite for main. pythonpath=. puts the repo root on
; sys.path so tests can `import experiments...` without living inside a package.
testpaths = tests
pythonpath = .
