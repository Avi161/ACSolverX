# AK(3) stable-proof campaign — collected results

Root: `/content/drive/MyDrive/ak3_stable_proof`

## ❌ HEADLINE: certified negative — 0 solved

**0 solves across the entire campaign.** 180,645 distinct Lemma-11 quotients harvested, 6,058 attempted by greedy substitution — all floor at min total length **13** (trivial = 2).

## Lane D (plateau elimination) — per box

| box | combos | unique candidates | attempted | solved | certs |
|---|---|---|---|---|---|
| D1 | 20 | 173,083 | 5,866 | 0 | 0 |
| D2 | 20 | 76,307 | 5,350 | 0 | 0 |
| D3 | 190 | 87,939 | 5,497 | 0 | 0 |

**Union across D1/D2/D3:** 180,645 distinct quotients (dedup by symmetry mkey), 6,058 attempted, 0 solved.

**Floor (min_total_len) histogram, union of all attempts:**
`13:16687  19:23  20:3`

## High-L cap-gap re-solve (resolve_hiL)

Re-solves the shortest candidates at a raised per-relator cap to test whether the L=24 limit causes false negatives. `max_rel_len` = longest single relator reached; if it stays < 24 the cap was never the binding constraint.

| arm (L) | attempted | solved | max_rel_len hist | floor hist |
|---|---|---|---|---|
| L=40 | 157 | 0 | `21:154  24:3` | `13:157` |

- L=40: peak relator length **24** → cap WAS pressed (max_rel_len up to 24) — headroom is load-bearing

## Grid probes (B/C)

- **B**: 6 probes, 0 solved/hit
- **C**: 8 probes, 0 solved/hit

## Data archive (reusable per-record datasets)

- `campaign_candidates.jsonl.gz` — 180,645 distinct quotients (relators + provenance)
- `campaign_trials.jsonl.gz` — 16,870 solve trials (0 solved; each row = relators + budget + cap + outcome)
- `campaign_grid_probes.jsonl.gz` — 14 B/C grid probes (StableSolver / dumb-stabilization / MITM, full per-probe record)
- `MANIFEST.md` — full schema + reuse notes

Every relator tested and the budget/cap it ran to is preserved here, so a future attempt can skip known negatives instead of recomputing them.

---
*Generated by collect_results.py — read-only aggregation of the Drive result tree.*
